# Experiments

Machine-readable experiment records are written by `ssa-archive experiment m1|m2` to `.local/dolphin-evidence/experiments/<id>.json` (schema `specs/001-ssa-level-research/contracts/experiment-record.schema.json`). They reference local game data and are not committed; this folder holds human-readable summaries and the reproduction procedure.

## Reproducing any experiment from its record

1. **Identity**: `inputs.game` must be the SSA dump `SSPP52` PAL revision 1 (`ssa-archive identify --game <wbfs>`).
2. **Original archive**: `inputs.archive_disc_path` with `inputs.original_sha256`; extract it with `ssa-archive disc-extract` and compare the hash.
3. **Workspace and rebuild**: `ssa-archive extract` then `ssa-archive rebuild` with the record's variant (`identical`, `sequential`, `reencode`) or, for M2, the `inputs.mutation` (`entry_index`, `offset`, `type`, `old_hex`, `new_hex`); the result must hash to `inputs.rebuilt_sha256`.
4. **Patch**: `inputs.patch_dir` and `inputs.descriptor` are regenerated by `ssa-archive patch --force`; descriptor paths use forward slashes.
5. **Runs**: replay `inputs.input_script` through `ssa-archive experiment m1|m2`; compare the `monitor_lines` (file-monitor sizes served for the patched path), the screenshots and `observed_effect` with the record. A run is only PASS with `crash_or_load_error: false` and, for M2, the predicted effect observed in every repeat.

## Entering the tutorial level (recorded 2026-09-12)

Script `tools/ssa-archive/src/experiments/input-scripts/level-027-entry.json`, recorded with `ssa-archive experiment explore` on the plain dump with Sonic Boom loaded at the title screen: wrist-strap screen → `A` → Activision logo → `A` → sky → `A` → title "Press A to begin" → figure load → `A` → autosave notice → `A` → Choose Play (Skyland Adventures) → `A` → Pick Game Slot (Save Game 1, 0 %) → `A` Continue Game → intro cinematic (balloon, Hugo) → `level/Level_027_Tutorial.arc` is read → Hugo dialogues advanced with `A` → "This Skylander is visiting from another adventure" prompt (`A` = No) → Sonic Boom stands next to Hugo; the Nunchuk stick moves the character. `Plus` opens the pause menu and is never used. Waits are wall-clock, tolerant to frame stalls during the 69 MB `Title.arc` load.

## Live memory probes (M2 candidate filtering)

`ssa-archive experiment live-probe --pattern <hex>... --poke match:<i>:<offset>=<value>[=<wait>] [--save-slot n]` boots the plain dump, enters the tutorial, searches MEM1/MEM2 for byte patterns copied from a decoded IGZ, writes a float at a found address and keeps before/after screenshots. Run `live-probe1` (2026-09-12) found the section-1 bytes of `level.bld` resident in MEM1 at `file offset + 0x80DBC020` and saved a native state in slot 6 at the tutorial start, which future probes can restore instead of replaying the 4.6 min entry script (state files live in `.local/dolphin-user/StateSaves/`, same game only, original level data).

## Runtime pointer scans (M3 convergence, 2026-09-13)

`ssa-archive experiment ptr-scan --file <decoded level.bld> --address <file offset>[,...] [--pattern <hex>] [--dimensions] --figure <.sky>` boots the plain dump, loads the figure, restores state slot 6 (tutorial start), snapshots MEM1 (and MEM2), reads back the resident bytes of each target object and diffs them against the file, then searches the snapshot for pointers to each target. `--dimensions` also dumps the whole resident object section to `.local/dolphin-evidence/<label>-section1.bin`; `igz fixups <decoded> <dump>` turns that dump into the complete fixup map (which words are pointers, ids, strings, class pointers; which objects the loader visited). One boot each, no archive mutation.

| Run | What it showed |
| --- | --- |
| `ptr-scan1` | Loader rewrites visited objects in place: type → class pointer, refcount → `0x0408000N`, id `+0x101C3408`, section-relative offsets → `0x80DBC020 + 0x4B180 + value`, strings interned in MEM1, flagged `0x8000000N` words → global table pointers. The spawn `tfbPhysicsModel` gains back-pointers to its type-92 owner (+0x6C, +0x118), heap links (+0x108, +0x134) and its position copied to +0xAC. The owner is referenced at runtime by the header table, by a heap pointer array (`0x8121EA30`) and by a heap object (`0x812299FC`). 100 heap copies of the spawn position triple exist (objects with class `0x80485BA0`). A heap descriptor at `0x81220634` lists section 1 in MEM1 and sections 2..8 in MEM2. |
| `ptr-scan2` | Full section dump: 21 408 / 21 680 objects rewritten; 272 untouched objects scattered among rewritten ones, 179 of them with no incoming pointer → the loader follows pointers from the section-1 header roots instead of walking the section; 157 690 pointer words, 38 584 id words identified. Basis of `igz clone-entity` (reachable clone with header-table registration). |

## Index

| Id | Kind | Archive | Variant / mutation | Status | Summary |
| --- | --- | --- | --- | --- | --- |
| `m1-level_027_tutorial-sequential-1789233029926` | M1_ROUNDTRIP | `level/Level_027_Tutorial.arc` | sequential + 1 padding block | **FAIL (consumption)** | Game booted from the descriptor and reached the tutorial, but the monitor served the original 52 733 kB: the XML used `external="files/…"`, which Dolphin resolves relative to the XML folder. Negative result that fixed the patch generator. |
| `m1-level_027_tutorial-sequential-1789233660781` | M1_ROUNDTRIP | `level/Level_027_Tutorial.arc` | sequential + 1 padding block (52 735 248 bytes, 486 222 classified diff regions, 0 unclassified) | **PASS** | Control served 52 733 kB, patched served 52 735 kB in all 8 monitor lines; tutorial reached with Sonic Boom (HUD, 312 HP), Nunchuk moves observed, 17 screenshots, bridge responsive, no alert in the log. |
| `m1-level_027_tutorial-reencode-1789234236226` | M1_ROUNDTRIP | `level/Level_027_Tutorial.bld` | reencode (all 7 LZMA entries) | **FAIL (boot)** | Control run complete (served 15 241 kB); the patched run lost the bridge on a screenshot at the Activision logo, during the 500 MB `permanent/global.arc` load, before the level was touched. Captures were then made as stall-tolerant as waits. |
| `m1-level_027_tutorial-reencode-1789234744424` | M1_ROUNDTRIP | `level/Level_027_Tutorial.bld` | reencode (15 331 434 bytes, 60 989 classified diff regions, 0 unclassified), control skipped (see previous record) | **PASS** | Patched served 15 331 kB; tutorial reached with the re-encoded nested `level.bld`, Sonic Boom recognised and moved, 17 screenshots, bridge responsive. Together with M1a this records **M1 = PASS** (`docs/m1-status.json`). |
| `live-probe1` | live memory probe | plain dump | patterns from `level.bld` section 1; poke X −259.26 → −159.26 at 0x80ED1BA0 | DONE (no visible effect) | Section-1 bytes resident in MEM1 at file offset + 0x80DBC020; write verified by read-back; scene unchanged at the start camera; native state saved in slot 6 at the tutorial start. |
| `ram-diff1`, `ram-diff2` | RAM diff (state slot 6, stick right then left) | plain dump | MEM1 snapshots | DONE | `ram-diff1` restored the state without a figure on the Portal (game waits for a Skylander; diffs were effects only). `ram-diff2` with Sonic Boom loaded first: live position (91.34, 10.55, 44.22) at 0x804F81E0 / 0x80512F00 / 0x80660640 moves (−6.3, +0.9, −7.5) on stick right and returns on stick left. |
| `m2-level_027_tutorial-e3-o3479944-1789236713623` | M2_MUTATION | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x351988 f32be 92.39 → 100.39 (ScriptSet position 1.2 units from the spawn, radius 5, heading 261) | **FAIL (observation)**, finding back to UNKNOWN | Control + 2 patched runs, mutated file served (15 331 kB), bytes verified; Sonic Boom spawned at the same spot in both runs. The ScriptSet is not the spawn point. |
| `m2-level_027_tutorial-e3-o1734528-1789237564649` | M2_MUTATION (screening, 1 run, no control) | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x1A7780 f32be 91.73 → 99.73 (`tfbPhysicsModel` position 0.7 units from the live spawn) | UNKNOWN by rule (single run), **effect observed** | Sonic Boom spawned visibly displaced toward screen left / the island edge, away from Hugo, with the camera following; mutated file served (15 331 kB). Promoted the finding to LIKELY; formal 2-repeat experiment follows. |
| `m2-level_027_tutorial-e3-o1734528-1789248814839` | M2_MUTATION (formal, 2 identical runs, control skipped) | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x1A7780 f32be 91.73 → 99.73 | **PASS** → **M2 = PASS** (`docs/m2-status.json`) | Both runs: Sonic Boom spawns displaced toward screen left near the island edge, Hugo, windmill and sunflowers unchanged, camera follows; mutated file served (15 331 kB) in both. Finding `level.physicsmodel.spawn-candidate` promoted to CONFIRMED and editable (size-preserving float). |
| `m3-level_027_tutorial-e3-1789250807367` | M3 duplication (2 identical runs, control skipped) | `level/Level_027_Tutorial.bld` entry 3: clone of the CONFIRMED `tfbPhysicsModel` spawn record appended to section 1 (+544 B, X 99.73, new id) and written into a null slot of the section-1 header list; chunk table grew (15 341 674 B) | **FAIL (observation)** → **M3 = FAIL** (`docs/m3-status.json`) | The cloned level loads and plays (served 15 341 kB, no alert), but the scene is identical to the control in both runs: appending an object and listing it does not instantiate it. Positive side results: the game tolerates an appended object, a grown chunk table and a modified list slot. Blocking unknown: the structure the engine walks to instantiate records. |
| `m2-level_027_tutorial-e3-o1137536-1789235411459` | M2_MUTATION | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x115B80 f32be −259.26 → −159.26 | **FAIL (observation)**, finding stays UNKNOWN | Pipeline test only: the mutated entry was re-encoded alone, the archive verified and fully classified, the game loaded the level (served 15 331 kB) and the tutorial was played; the predicted displacement was not observed. Proves the M2 tooling end to end, not a world property. |
