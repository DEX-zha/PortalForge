# Experiments

Machine-readable experiment records are written by `ssa-archive experiment m1|m2` to `.local/dolphin-evidence/experiments/<id>.json` (schema `specs/001-ssa-level-research/contracts/experiment-record.schema.json`). They reference local game data and are not committed; this folder holds human-readable summaries and the reproduction procedure.

## Scripted Barrel movement, 2026-09-15

`barrel-placement-2026-09-15` records two identical positive boots (`barrel-control-1789474472958`,
`barrel-control-1789475022808`) and the later resource-only comparison (`barrel-resource-1789475419544`).
Moving Barrel(6) renders a barrel on the start island; moving the stored Barrel resource does not reproduce that
effect in the single comparison. All three replacements were consumed and owned Dolphin processes stopped normally.
The normalized local record validates against the experiment schema. Its mutation spans three adjacent f32be words;
its control uses a different resource-only patch, not an untouched baseline. Follow
[the exact positions, hashes and editor procedure](../editor/scripted-movement.md) to reproduce it.

## Reproducing any experiment from its record

1. **Identity**: `inputs.game` must be the SSA dump `SSPP52` PAL revision 1 (`ssa-archive identify --game <wbfs>`).
2. **Original archive**: `inputs.archive_disc_path` with `inputs.original_sha256`; extract it with `ssa-archive disc-extract` and compare the hash.
3. **Workspace and rebuild**: `ssa-archive extract` then `ssa-archive rebuild` with the record's variant (`identical`, `sequential`, `reencode`) or, for M2, the `inputs.mutation` (`entry_index`, `offset`, `type`, `old_hex`, `new_hex`); the result must hash to `inputs.rebuilt_sha256`.
4. **Patch**: `inputs.patch_dir` and `inputs.descriptor` are regenerated by `ssa-archive patch --force`; descriptor paths use forward slashes.
5. **Runs**: replay `inputs.input_script` through `ssa-archive experiment m1|m2`; compare the `monitor_lines` (file-monitor sizes served for the patched path), the screenshots and `observed_effect` with the record. A run is only PASS with `crash_or_load_error: false` and, for M2, the predicted effect observed in every repeat.

## Entering the tutorial level (recorded 2026-09-12)

Script `tools/ssa-archive/src/experiments/input-scripts/level-027-entry.json`, recorded with `ssa-archive experiment explore` on the plain dump with Sonic Boom loaded at the title screen: wrist-strap screen → `A` → Activision logo → `A` → sky → `A` → title "Press A to begin" → figure load → `A` → autosave notice → `A` → Choose Play (Skyland Adventures) → `A` → Pick Game Slot (Save Game 1, 0 %) → `A` Continue Game → intro cinematic (balloon, Hugo) → `level/Level_027_Tutorial.arc` is read → Hugo dialogues advanced with `A` → "This Skylander is visiting from another adventure" prompt (`A` = No) → Sonic Boom stands next to Hugo; the Nunchuk stick moves the character. `Plus` opens the pause menu and is never used. Waits are wall-clock, tolerant to frame stalls during the 69 MB `Title.arc` load.

## Live memory probes (M2 candidate filtering)

`ssa-archive experiment live-probe --pattern <hex>... --poke match:<i>:<offset>=<value>[=<wait>] [--save-slot n]` boots the plain dump, enters the tutorial, searches MEM1/MEM2 for byte patterns copied from a decoded IGZ, writes a float at a found address and keeps before/after screenshots. Run `live-probe1` (2026-09-12) found the section-1 bytes of `level.bld` resident in MEM1 at `file offset + 0x80DBC020` and saved a native state in slot 6 at the tutorial start, which future probes can restore instead of replaying the 4.6 min entry script (state files live in `.local/dolphin-user/StateSaves/`, same game only, original level data).

## Runtime pointer scans (M3 convergence, 2026-09-13)

`ssa-archive experiment ptr-scan --file <decoded level.bld> --address <file offset>[,...] [--pattern <hex>] [--dimensions] --figure <.sky>` boots the plain dump, loads the figure, restores state slot 6 (tutorial start), snapshots MEM1 (and MEM2), reads back the resident bytes of each target object and diffs them against the file, then searches the snapshot for pointers to each target. `--dimensions` also dumps the whole resident object section to `.local/dolphin-evidence/<label>-section1.bin`; `igz fixups <decoded> <dump>` turns that dump into the complete fixup map (which words are pointers, ids, strings, class pointers; which objects the loader visited). One boot each, no archive mutation.

| Run | What it showed |
| --- | --- |
| `ptr-scan1` | Loader rewrites visited objects in place: type → class pointer, refcount → `0x0408000N`, id `+0x101C3408`, section-relative offsets → `0x80DBC020 + 0x4B180 + value`, strings interned in MEM1, flagged `0x8000000N` words → global table pointers. The spawn `tfbPhysicsModel` gains back-pointers to its type-92 owner (+0x6C, +0x118), heap links (+0x108, +0x134) and its position copied to +0xAC. The owner is referenced at runtime by the header table, by a heap pointer array (`0x8121EA30`) and by a heap object (`0x812299FC`). 100 heap copies of the spawn position triple exist (objects with class `0x80485BA0`). A heap descriptor at `0x81220634` lists section 1 in MEM1 and sections 2..8 in MEM2. |
| `ptr-scan2` | Full section dump: 21 408 / 21 680 objects rewritten; 272 untouched objects scattered among rewritten ones, 179 of them with no incoming pointer → the loader follows pointers from the section-1 header roots instead of walking the section; 157 690 pointer words, 38 584 id words identified. Basis of `igz clone-entity` (reachable clone with header-table registration). |

## Index

| Id | Kind | Archive | Variant / mutation | Status | Summary |
| --- | --- | --- | --- | --- | --- |
| `m1-level_027_tutorial-sequential-1789233029926` | M1_ROUNDTRIP | `level/Level_027_Tutorial.arc` | sequential + 1 padding block | **FAIL (consumption)** | Game booted from the descriptor and reached the tutorial, but the monitor served the original 52 733 kB: the XML used `external="files/…"`, which Dolphin resolves relative to the XML folder. Negative result that fixed the patch generator. |
| `m1-level_027_tutorial-sequential-1789233660781` | M1_ROUNDTRIP | `level/Level_027_Tutorial.arc` | sequential + 1 padding block (52 735 248 bytes, 486 222 classified diff regions, 0 unclassified) | **PASS** | Control served 52 733 kB, patched served 52 735 kB in all 8 monitor lines; tutorial reached with Sonic Boom (HUD, 312 HP), Nunchuk moves observed, 17 screenshots, bridge responsive, no alert in the log. |
| `m1-level_027_tutorial-reencode-1789234236226` | M1_ROUNDTRIP | `level/Level_027_Tutorial.bld` | reencode (all 7 LZMA entries) | **FAIL (boot)** | Control run complete (served 15 241 kB); the patched run lost the bridge on a screenshot at the Activision logo, during the 500 MB `permanent/global.arc` load, before the level was touched. Captures were then made as stall-tolerant as waits. |
| `m1-level_027_tutorial-reencode-1789234744424` | M1_ROUNDTRIP | `level/Level_027_Tutorial.bld` | reencode (15 331 434 bytes, 60 989 classified diff regions, 0 unclassified), control skipped (see previous record) | **PASS** | Patched served 15 331 kB; tutorial reached with the re-encoded nested `level.bld`, Sonic Boom recognised and moved, 17 screenshots, bridge responsive. Together with M1a this records **M1 = PASS** (`docs/m1-status.json`). |
| `live-probe1` | live memory probe | plain dump | patterns from `level.bld` section 1; poke X −259.26 → −159.26 at 0x80ED1BA0 | DONE (no visible effect) | Section-1 bytes resident in MEM1 at file offset + 0x80DBC020; write verified by read-back; scene unchanged at the start camera; native state saved in slot 6 at the tutorial start. |
| `ram-diff1`, `ram-diff2` | RAM diff (state slot 6, stick right then left) | plain dump | MEM1 snapshots | DONE | `ram-diff1` restored the state without a figure on the Portal (game waits for a Skylander; diffs were effects only). `ram-diff2` with Sonic Boom loaded first: live position (91.34, 10.55, 44.22) at 0x804F81E0 / 0x80512F00 / 0x80660640 moves (−6.3, +0.9, −7.5) on stick right and returns on stick left. |
| `m2-level_027_tutorial-e3-o3479944-1789236713623` | M2_MUTATION | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x351988 f32be 92.39 → 100.39 (ScriptSet position 1.2 units from the spawn, radius 5, heading 261) | **FAIL (observation)**, finding back to UNKNOWN | Control + 2 patched runs, mutated file served (15 331 kB), bytes verified; Sonic Boom spawned at the same spot in both runs. The ScriptSet is not the spawn point. |
| `m2-level_027_tutorial-e3-o1734528-1789237564649` | M2_MUTATION (screening, 1 run, no control) | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x1A7780 f32be 91.73 → 99.73 (`tfbPhysicsModel` position 0.7 units from the live spawn) | UNKNOWN by rule (single run), **effect observed** | Sonic Boom spawned visibly displaced toward screen left / the island edge, away from Hugo, with the camera following; mutated file served (15 331 kB). Promoted the finding to LIKELY; formal 2-repeat experiment follows. |
| `m2-level_027_tutorial-e3-o1734528-1789248814839` | M2_MUTATION (formal, 2 identical runs, control skipped) | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x1A7780 f32be 91.73 → 99.73 | **PASS** → **M2 = PASS** (`docs/m2-status.json`) | Both runs: Sonic Boom spawns displaced toward screen left near the island edge, Hugo, windmill and sunflowers unchanged, camera follows; mutated file served (15 331 kB) in both. Finding `level.physicsmodel.spawn-candidate` promoted to CONFIRMED and editable (size-preserving float). |
| `m3-level_027_tutorial-e3-1789250807367` | M3 duplication (2 identical runs, control skipped) | `level/Level_027_Tutorial.bld` entry 3: clone of the CONFIRMED `tfbPhysicsModel` spawn record appended to section 1 (+544 B, X 99.73, new id) and written into a null slot of the section-1 header list; chunk table grew (15 341 674 B) | **FAIL (observation)** → **M3 = FAIL** (`docs/m3-status.json`) | The cloned level loads and plays (served 15 341 kB, no alert), but the scene is identical to the control in both runs: appending an object and listing it does not instantiate it. Positive side results: the game tolerates an appended object, a grown chunk table and a modified list slot. Blocking unknown: the structure the engine walks to instantiate records. |
| `m3-level_027_tutorial-e3-1789253576009` | M3 reachable clone (screening, 1 run, no control) | `level/Level_027_Tutorial.bld` entry 3: owner+physics block [0x1A7658, 0x1A7900) copied to the end of section 1, registered as header-table entry #2866 with a **4-byte** shift of the object area (160 575 pointers rebased); 15 337 578 B served | **FAIL (load)** | The level never left the "Loading…" screen (user saw the freeze); RAM probes found the clone's physics header still raw (`0000007B`) and the original's in a half-processed state (`0408007B 00E071A1`). Causes identified offline: 335 runtime-adjusted pointer words were not rebased, and the 4-byte shift broke the 32-byte alignment of every object (inline geometry/texture data). Fixed by `igz fixups` adjusted-pointer rule and a 32-byte registration shift with an alignment-preserving pre-pad. |
| `m3-level_027_tutorial-e3-1789254290299` | M3 reachable clone (screening, 1 run, no control) | same block, header-table entry #2866 with a **32-byte** shift (alignment preserved, 1 555 runtime-adjusted pointers added); 15 337 578 B served | **FAIL (load)** | Frozen on "Loading…" again. The RAM probe caught the original spawn record in an intermediate loader state (`80E0721B 00000001 911D20C8 80E071A0`): every pointer word had been rebased with a section-indexed encoding (high byte = target section, `0x01xxxxxx` = section-2 strings), which exposed that header word +8 is a shared string pointer, not an id, and that the other sections may hold pointers into section 1. |
| `ptr-scan3` | pointer scan with MEM1+MEM2 dumps (state slot 6) | plain dump | resident copies of all 9 sections | DONE | Sections 2, 4, 6, 8 are untouched at load; section 3 changes 82 words; section 7 holds objects (473 class pointers, 72 self pointers); **section 5 is a second object section** (4 584 class pointers, 7 582 self pointers) with **2 320 plain pointers into section 1** that the first two relocations did not rebase. `igz fixups --regions` now includes them (`cross_pointer_words`). |
| `m3-level_027_tutorial-e3-1789255841154` | M3 reachable clone (screening, 1 run, no control) | same block, 32-byte shift, section-1 **and section-5** pointers rebased (328 902 words) | **FAIL (load)** | Frozen on "Loading…" a third time with the same intermediate state on the original record: the shift was not the (only) cause. |
| `m3-level_027_tutorial-e3-1789256232052` | M3 diagnostic (no relocation) | header-table entry #1074 redirected from the original spawn owner to the appended clone | **FAIL (load)**, decisive | Frozen on "Loading…", but the RAM probes show the **original** owner and physics model fully constructed (class pointer `80486514`, position copied to +0xAC) although no table entry points at them any more, and the **clone raw**. Table entries therefore do not drive construction; a table entry pointing at an unconstructed object breaks the post-construction table processing. Refcount analysis (header word +4 = number of owning pointers, table excluded) confirms the table is not an owning reference. |
| `m3-level_027_tutorial-e3-1789256664691` | M3 diagnostic (append only) | owner block appended after the last object, no table entry, no shift | loads (UNKNOWN by rule), decisive | Level alive (132 heap copies of the spawn position) but the appended clone stays **raw**: the loader enumerates the original object range and stops at its end, so nothing after the six tail objects is ever constructed. Consequence: a clone must be inserted *inside* the range (`igz clone-entity --insert-before`). |
| `m3-level_027_tutorial-e3-1789257261106` | M3 reachable clone (inside the span) | owner block inserted before the tail objects (0x457588), all section-1 and section-5 pointers rebased, 5 shared-target refcounts bumped, no table entry | **FAIL (load)**, most advanced | The clone **owner** was fully constructed in RAM (class pointer `80485F48`, id remapped, +0x18 rebased) — being inside the enumerated span is what makes a record instantiate — but its embedded `tfbPhysicsModel` child stayed raw and the load hung. The remaining unknown is what the child constructor trips on. Resident section dump saved for offline analysis. |
| `m3-level_027_tutorial-e3-1789258792820` | M3 reachable clone (inside the span + table entry) | clone inside the span AND registered as header-table entry #2866 (32-byte global shift, all pointers rebased in-file, refcounts bumped) | **FAIL (load)**, decisive | Froze far earlier than the no-table run: only 1 036 of 21 680 records constructed, header type words wrongly rebased (physics +0 `0x7b` → `base+0x7b`). Growing the table shifts every object and **desynchronises the game's pointer-relocation table globally**. Proves the game rebases pointers from an explicit relocation table (ignoring the file's hand-rebased bytes), which the clone is absent from — the sole remaining M3 blocker (finding `igz.loader.relocation-table`). |
| `m3-level_027_tutorial-e3-1789261575772` | M3 in-place overwrite (no shift, no table growth) | clone written over the type-92 record at 0x3EDEFC (its existing table entry registers it); file length unchanged, only the target blob + 5 refcount words differ | **FAIL (load)**, decisive | The cleanest registration froze too. The original spawn constructed normally (`80486514`), but the clone`s position (99.73) was absent from MEM1 and the load hung on "Loading". Registering a same-type record in a valid slot is necessary but not sufficient: duplicating a type-92 entity is blocked by a deeper construction constraint that in-place M2 mutation never triggers. Every registration path (grow table, redirect entry, insert+register, in-place overwrite) freezes. |
| `m3-level_027_tutorial-e3-1789299359139` | M3 generic entity, chain insertion inside the span | `PlacementReference` 0x123138 (pure chain node, only pointer = next at +0x64) copied to 0x457588 before the six tail roots, `P.next` -> copy, `copy.next` -> old next; no table change | **FAIL (load)**, but **first constructed clone** | The loader finished the level (21 402 constructed = healthy) and **the copy was constructed** (class `0x8049C738` reached through `P.next`), yet the six tail roots stayed raw and the post-load init hung. Cause pinned by the dump: the copy sat inside the blob of the type-52 table record 0x457548, whose constructor walked its enlarged blob in 0x40 strides and stomped the copy (class pointers at +0x1C/+0x40/+0x5C, `TFL8` -> refcount). Rule: any insertion inside the span is walked by the enclosing record; place copies at the section end (findings `igz.loader.blob-walk-stomps-insertions`, `igz.loader.reflection-constructs-pointees`). |
| `m3-level_027_tutorial-e3-1789299899074` | M3 generic entity, append + chain link | same `PlacementReference` copy appended at the section end (0x4579C0, no record moved, no table change), `P.next` -> copy, `copy.next` -> old next | **loads and plays (no freeze)**, clone inert | First cloned level that plays: tail roots constructed, Sonic Boom in the tutorial. `P.next` is rebased to the copy (`0x812139E0`) but the copy header stays raw (`0x0000000C`): reflection rebases pointers but does not construct pointees; construction is the positional walk of table-record blobs. A raw pointee is tolerated. Next: insert the copy inside P`s own blob so its owner`s walker constructs it. |
| `m3-level_027_tutorial-e3-1789300434156` | M3 generic entity, chain insertion in the head span | same `PlacementReference` copy inserted at N (0x1231A8), N and 20 007 later records shifted 0x70, all pointers rebased, no table change | **FAIL (load)**, copy constructed | The copy and the whole head span constructed (class `0x8049C738`), but the type-52 array walker started at the OLD first-table-record address (0x123598, 0x70 short of the rebased entry): head-span construction is count-bounded, so an inserted record shifts every later walker by one record; 10 397 table records raw, tail roots raw, hang (finding `igz.loader.head-span-count-walk`). Next: same-size replacement instead of insertion. |
| `m3-level_027_tutorial-e3-1789301273731` | M3 generic entity, **same-size replacement** (zero shift) | `PlacementReference` 0x123138 copied over its successor 0x1231A8 (6 words changed in the whole file; refcount 1; next = victim`s old next) | **PLAYS — duplicate constructed** (screening, 1 run) | The copy is constructed as a PlacementReference (`0x8049C738` at the slot, reached through `P.next`) and processed by the post-load phase like an original; 21 407 records constructed (= healthy), tail roots constructed, Sonic Boom in the tutorial. Two live instances of P coexist in the chain. M3 mechanism achieved; the subject is a texture reference (`checker.png`), so no visible change; the victim (`focus_mapper.png` reference) is lost. Formal PASS needs 2 repeats (finding `level.entity.duplicate-instantiated`). |
| `m3-level_027_tutorial-e3-1789301970935` | M3 duplication, **formal** (2 identical runs, control skipped: observed in the screening) | `PlacementReference` 0x123138 copied over its successor 0x1231A8 (`igz clone-entity --replace-node next`; 6 words changed, zero shift) | **PASS → M3 = PASS** (`docs/m3-status.json`) | Both runs: level plays, slot `0x8049C738` through `P.next`, chain `pred -> P -> copy -> 0x123218` constructed, tail roots constructed. Findings `level.entity.duplicate-instantiated` and `level.placement.linked-chain` promoted to CONFIRMED. The duplicate is a texture reference (invisible); visible duplication is the next chantier. |
| `m3-level_027_tutorial-e3-1789303984407` | Visible duplication, **screening** (1 run, control skipped: the two-sunflower control is in every earlier tutorial screenshot) | `ScriptSet` 0x34959C "sunflower_Template(1)" copied over `ScriptSet` 0x34AC18 "weed_2_Template(8)" inside the 027_OpeningCS_Controller.ai blob (`igz clone-entity --replace-record --keep 50,128,1ac,1b0,1b8`, position +0x6C -> (85.5, 10, 42); 10 words changed, zero shift) | **UNKNOWN** (SC-004: screening, matched once) | Level plays; **three sunflowers** by the windmill instead of two (screenshot `-46-tutorial-skylander.png`). First visible duplication. Finding `level.prop.scriptset-placement` -> LIKELY. Formal 2-run confirmation next. |
| `m3-level_027_tutorial-e3-1789304493862` | Visible duplication, **formal** (2nd identical run of the sunflower-over-weed file, control skipped) | same file and plan as `1789303984407` | **matched 2/2** | Identical: level plays, three sunflowers by the windmill. Finding `level.prop.scriptset-placement` -> **CONFIRMED**. Generalisation runs (G1/G2/G3) follow. |
| `m3-level_027_tutorial-e3-1789304824550` | Generalisation G1+G3, **screening** (1 run) | G1: `ScriptSet` 0x34AC18 "weed_2_Template(8)" copied over the `ScriptSet` 0x34959C "sunflower_Template(1)" slot (`--replace-record --keep 50,128,1ac,1b0,1b8`, +0x6C -> (85.5, 10, 42)); G3: `ScriptSet` 0x34AA34 "sunflower_Template(2)" (size 0x1E4 variant, model by reference) moved in place +0x6C (79.68, 10.46, 40.94) -> (88, 10.3, 44) | **matched** (screening) | Level plays; a weed stands where the extra sunflower stood, the sunflower(1) is gone, the remaining sunflower moved left in front of the windmill, weed(8) unchanged. The mechanism is not sunflower-specific: different model, and the +0x6C handle holds on the 0x1E4 layout variant. |
| `m3-level_027_tutorial-e3-1789305160219` | Generalisation G2, **screening** (1 run) | `ScriptSet` 0x232258 "Push_Block_Template(1)" (script Remove_Stuf_at_start.ai, push-block class, +0xF0 behaviour script) copied over the `ScriptSet` 0x2D2844 "CS_Closing01a" slot (script 027_Level_End_Temp.ai, camera-marker class), name kept, +0x6C -> (85.5, 10, 42); 17 words changed | **FAIL** (observation) | Level loads, opening cutscene starts, then **freezes in-game** (first in-game freeze; earlier ones were at load). No block visible. Three variables changed at once (other script, other template class at +0x1C, end-cutscene slot); script sharing is not the cause (Template(1) and (2) already share script 0x20B6D8 in the original). Isolating test G2b follows: same script, same class, same size. |
| `m3-level_027_tutorial-e3-1789305543965` | Generalisation G2b, isolating (1 run) | `ScriptSet` 0x20B598 "Push_Block_Template(2)" copied over the `ScriptSet` 0x1E0490 "PushBlock_Mabu" slot — same script Remove_Stuf_at_start.ai, same class 0x1B1A58, same size 0x140, slot name + own script (+0xF0) + companion (+0x10C) kept; +0x6C -> (85.5, 10, 42); 15 words changed | **FAIL** (observation) | **Freezes in-game at the same moment as G2.** Cross-script/cross-class is NOT the cause. Common factor: a push block placed on the start island at (85.5, 10, 42) during the opening cutscene. Control needed: in-place M2 move of PushBlock_Mabu to the same spot (no replacement). |
| `m2-level_027_tutorial-e3-o3296360-1789305999607` | Generalisation G4, **screening** (1 run): in-place move of a NON-plant, non-script-wrapper placement | type-104 placement 0x324C44 "Windmill_Blades2" (layer Jump pads_PushBLock_Gates, WindmillBlades.mdl, 027_WindmillProp.ai): +0x24 x 85.52 -> 79.5 (`experiment m2 --offset 3296360 --type f32be --value 79.5`) | **matched** (screening) | The windmill blades render detached from the hub, displaced to the screen-right as predicted; level plays. The type-104 +0x24 handle generalises beyond plants and beyond the type-111 wrapper. |
| `m2-level_027_tutorial-e3-o3296360-1789307182968` | Generalisation G4, **formal** (2nd identical run of the windmill-blades move) | same mutation as `1789305999607` (rebuilt sha256 identical) | **matched 2/2** (PASS via `--replicates`) | Identical: blades detached from the hub, level plays. Finding `igz.placement.type104-record` -> **CONFIRMED**: the type-104 record is the editable placement (position/rotation/scale/behaviour/model/layer). |
| `m3-level_027_tutorial-e3-1789315647217` | **t104-generic duplication**, screening (1 run) | `edit replace 0x3D64E4 --over 0x3D3E44 --pos 85.5,10,42 --scale 250`: the standalone type-104 placement `weed_flowerBTemplate(5)` copied over the `weed_2_Template(16)` slot (same span 0x19C, same layer Plants, no type-111 wrapper on either side; slot name and refcount kept, model pointer taken from the source). 8 words changed: 7 inside the victim + 1 refcount bump; `shared_records` empty | **matched** (screening) | Level plays, no freeze. A large pink flowerB plant renders at (85.5, 10, 42) where the baseline shows bare ground, verified by pixel diff against run `1789301970935` (33x17 px cluster at x538,y239). The duplication recipe therefore works without any type-111 wrapper and without touching a shared record. |
| `m3-level_027_tutorial-e3-1789323431499` | **First edit made entirely through the 3D editor**, formal (2 runs, control skipped) | `sunflower_Template(1)` at `0x3495E4` moved straight up, y 9.962 -> 16, through the editor's own chain: intent, in-memory edit, save with its plan checked against the written bytes, replacement-only patch, launch with the prediction stated first. Five words changed, none outside the placement | **PASS 2/2** | Both runs: the level loads and plays, the sunflower hangs high above its normal spot with its head at the top of the frame, its twin unchanged. The editor's save, patch and launch chain is demonstrated in game. Two earlier boots were spent and produced no record: the launch endpoint held the HTTP response open past the client timeout (now fixed, it is polled) and the second was interrupted by an accidental window close. |
| `m2-level_027_tutorial-e3-o2847700-1789303050823` | M2_MUTATION (screening, 1 run, no control) | `level/Level_027_Tutorial.bld` entry 3 @0x2B73D4 f32be 92.69 → 100.69 (4th waypoint of the OpUseMacro "move to" in `027_HugoCS.ai`) | UNKNOWN by rule, **visible effect observed by the user** | Hugo walked to the displaced waypoint during the intro cutscene, then returned to his original spot when the cutscene ended (post-cutscene position set elsewhere). First script-ARGUMENT position confirmed visually; finding `level.hugo.cutscene-waypoints` → LIKELY. Decoded with `igz script` (script model 157/157). |
| `m2-level_027_tutorial-e3-o1137536-1789235411459` | M2_MUTATION | `level/Level_027_Tutorial.bld` entry 3 (`level.bld`) @0x115B80 f32be −259.26 → −159.26 | **FAIL (observation)**, finding stays UNKNOWN | Pipeline test only: the mutated entry was re-encoded alone, the archive verified and fully classified, the game loaded the level (served 15 331 kB) and the tutorial was played; the predicted displacement was not observed. Proves the M2 tooling end to end, not a world property. |
