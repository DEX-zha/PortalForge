# IGA version 4 (Skylanders: Spyro's Adventure, Wii) — container findings

This page is generated by `ssa-archive findings render` from the finding records in `docs/findings/records/` (category `container`, then `igz-objects`); edit the records, not this file. The prose below is maintained in `docs/iga-v4.intro.md`.

Confidence labels per FR-013: **CONFIRMED** (observed on local data and exercised by code or tests), **LIKELY** (concordant observations, not yet exercised), **UNKNOWN** (hypothesis). Nothing here was derived from the game code; everything comes from the archives themselves and from the community reader [igArchiveExtractor](https://github.com/NefariousTechSupport/igArchiveExtractor), which reads but cannot rebuild this version.

Local samples (game data, not committed): `character/001_Gryphon.arc` (42 entries), `character/001_Gryphon.bld` (7), `level/Level_000_Mining.bld` (7), `level/Level_027_Tutorial.arc` (477), `level/Level_027_Tutorial.bld` (7), `level/Challenge_Level_000.bld`, `level/Challenge_Level_001.bld`. All container fields are little-endian except the per-chunk compressed size (big-endian u16). Every sample rebuilds byte-identically from an extracted workspace (`tools/ssa-archive/tests/iga-fixtures.test.mjs`); every compressed entry decodes to an IGZ file and re-encodes into an archive that decodes identically (`tests/decode.test.mjs`).

Open questions are the UNKNOWN rows below and the IGZ structure needed for M2.

## Findings — IGA v4 container fields and chunk encoding

Generated by `ssa-archive findings render` from `docs/findings/records/`. Do not edit by hand. Labels: CONFIRMED (experimentally demonstrated), LIKELY (concordant observations), UNKNOWN (hypothesis). Only CONFIRMED findings may be editable.

| Id | Structure | Location | Type | Endian | Meaning | Evidence | Confidence | Editable | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| iga-v4.chunk-format | Compressed chunk | **/*.bld @ entry start + table offset (7) | u16 BE size + 5-byte LZMA props + stream | be | 0x8000-byte chunks; props always 5D 00 80 00 00 (lc3 lp0 pb2, dict 0x8000); next chunk 0x800-aligned; raw chunks when compression does not help | (research-probes/probe-chunks.mjs) Every decoded chunk carried these props; sizes consistent with alignment<br>(src/iga/decode.mjs) Tutorial.bld 945 chunks decode in 1.8 s | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.chunk-tables | Chunk tables for LZMA entries | **/*.bld @ after entry table (2) | u16 values / [flags,units] records | le | Multi-chunk: N+1 u16 at index mode&0xFFFFFF, value = (compressed?0x8000:0)\|offset/0x800, last = end; single-chunk: 2-byte [0x80\|flags][units] at byte offset mode&0xFFFFFF in the record area | (research-probes/probe-chunks.mjs) Full decode of 63 chunks incl. 6 raw chunks; records 80 01/02/03 match stored spans<br>(tests/decode.test.mjs) Re-encoded entry decodes; untouched entries unchanged | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.entry-table | Entry descriptors | **/*.arc, **/*.bld @ 0x30 + 4*count (12) | {u32 start, u32 size, u32 mode} | le | start absolute and 0x800-aligned, size uncompressed, mode high byte 0xFF stored / 0x10 LZMA chunked, low 24 bits locate the chunk table | (src/iga/reader.mjs) 533 entries consistent; byte-identical rebuild of 3 archives<br>(igArchiveExtractor IGAE_Globals.cs) LocalHeaderLength 0x0C, ChecksumLocation 0x30 for version 4 | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.hash-algorithm | Hash function of entry names | **/*.arc, **/*.bld @ 0x30 (4) | u32 | le | Unknown; 533 name/hash pairs available locally for offline recovery; only needed for new or renamed entries (M3+) |  | **UNKNOWN** | no | 2026-09-12 |
| iga-v4.hash-table | Name hash table | **/*.arc, **/*.bld @ 0x30 (4) | u32[count] | le | One 32-bit hash per entry in entry order, strictly ascending; algorithm unknown; copied verbatim on rebuild | (src/iga/header.mjs) Sorted on all samples (533 pairs) | **LIKELY** | no | 2026-09-12 |
| iga-v4.header.chunk-counts | Header words 0x24/0x28 | **/*.bld @ 0x24 (8) | u32 x2 | le | 0x24 = number of u16 values in the multi-chunk table area (chunks+1 per multi-chunk entry); 0x28 = byte size of the single-chunk record area (2 per single-chunk entry) | (research-probes/probe-chunks.mjs) 41/779/946 values match 40/778/945 decoded chunks; 12 bytes = 6 .pak records<br>(src/iga/decode.mjs + tests/decode.test.mjs) Decoder driven by these words decodes 7/7 entries of three .bld | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.header.magic-version | IGA v4 header magic and version | **/*.arc, **/*.bld @ 0x0 (8) | u32 x2 | le | Magic 0x1A414749 ("IGA\x1A") followed by version 4 | (tools/ssa-archive/tests/iga-fixtures.test.mjs) All 7 local samples parse; verify rejects other magics/versions<br>(igArchiveExtractor IGA_Version.cs) SkylandersSpyrosAdventureWii = 0x04 | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.header.name-table | Header words 0x18/0x1C | **/*.arc, **/*.bld @ 0x18 (8) | u32 x2 | le | Absolute offset and size of the name table; offset + size == file size | (src/iga/reader.mjs) Holds on all 7 samples; names resolve for 533 entries | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.header.table-size | Header word 0x08 | **/*.arc, **/*.bld @ 0x8 (4) | u32 | le | Size of hash table + entry table + chunk tables following the 0x30 header | (research-probes/probe-iga-header.mjs) count*16 on both .arc; .bld values equal tables + chunk area<br>(src/iga/reader.mjs) Bounds the chunk area; all 7 samples VALID and decode fully | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.header.word-0x10 | Header word 0x10: hash lookup scale | **/*.arc, **/*.bld @ 0x10 (4) | u32 | le | floor(0xFFFFFFFF / count): divisor turning a name hash into an estimated index in the sorted hash table (interpolation lookup). Must be recomputed whenever the entry count changes. | (research-probes/survey-level-headers.ps1 + probe-word10-formula.mjs) 152/152 level archives satisfy word = floor(0xFFFFFFFF/count) (counts 6 to 1080)<br>(src/iga/reader.mjs LOOKUP_WORDS_MISMATCH) All 7 local samples VALID with the check enforced | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.header.word-0x14 | Header word 0x14: maximum probe distance | **/*.arc, **/*.bld @ 0x14 (4) | u32 | le | max over entries of \|i - min(count-1, floor(hash_i / word0x10))\|: how far the interpolation estimate can be from the real index. Must be recomputed whenever hashes change. | (research-probes/probe-word10-formula.mjs) Matches on 7/7 local samples including 14 for the 477-entry arc and 2 for every 7-entry bld<br>(src/iga/reader.mjs LOOKUP_WORDS_MISMATCH) Enforced by verify; all samples VALID | **CONFIRMED** | no | 2026-09-12 |
| iga-v4.trailer | 32-byte record after the tables | **/*.arc, **/*.bld @ 4-aligned end of tables (32) | u32 x8 | le | EC4D4100 01000001 FFFFFFFF 00008000 FFFFFFFF x4; word 3 equals the chunk size; preserved verbatim as a raw gap | (workspace manifest raw_gaps) Identical in tutorial-arc and gryphon-bld workspaces | **LIKELY** | no | 2026-09-12 |

## Findings — IGZ object types and file structure

Generated by `ssa-archive findings render` from `docs/findings/records/`. Do not edit by hand. Labels: CONFIRMED (experimentally demonstrated), LIKELY (concordant observations), UNKNOWN (hypothesis). Only CONFIRMED findings may be editable.

| Id | Structure | Location | Type | Endian | Meaning | Evidence | Confidence | Editable | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| igz.detection.heuristic-limits | Which parts of the static object graph are trustworthy | level/*.bld:level.bld (decoded) @ n/a (1) | audit of the object-graph heuristics against runtime truth (ptr-scan3 dump + fixup map) | be | Checked against the resident image: (a) header detection {type<224, refcount==1, 0x01xxxxxx} yields 272 records the loader never touches; all heuristic tfbActorInfo (15), CameraInfo (15), AbstractPlacement (6) and ActorWaypoint (7) headers are among them, with zero inbound confirmed pointers — they are FALSE headers (bytes inside other records), not objects; (b) the container decoder (igz containers/members, T032) is almost entirely noise: of 8 549 "pointer lists with free capacity", 8 346 have element words that are not confirmed pointers and 8 345 a "pointer field" that is not a pointer; (c) runtime-derived lists (runs of >=3 confirmed pointer words designated by a confirmed pointer with a count before it) number 609, hold embedded sub-records rather than top-level entities, and none has spare capacity; (d) trustworthy: PlacementReference (698/704 constructed) and tfbSpriteInfo (4 301/4 302) are real multi-instance objects; the header table, the fixup map (pointer/id/class words) and the visited flags are runtime ground truth. Rule: any structural edit must be justified by runtime-confirmed data (table entries, confirmed pointer words, visited flags), never by header/container heuristics alone. | (scratch lists-free.mjs / lists-runtime.mjs / entities-owners.mjs over ptr-scan3-fixups.json + ptr-scan3-section1.bin) heuristic lists: 0/8549 pass runtime checks; false-header types have 0 visited / 0 inbound; PlacementReference 698/704 visited<br>(igz fixups (ptr-scan2/3)) 272 unvisited detected "objects" scattered among 21 408 visited ones | **LIKELY** | no | 2026-09-13 |
| igz.header.section-table | IGZ v5 header and section table | level/*.bld:level.bld, *.pak (decoded) @ 0x10 (16) | {u32 offset, u32 size, u32 alignment, u32 index} x N, zero-terminated | be | After magic 49475A01 and version 5 (two zero words), a table of 4-word section descriptors; the tutorial level.bld has 9 sections (alignments 0x800, 0x20, 2, 0x10, 0x20, 4, 0x20, 0x10, 0x10) and the last section ends exactly at the file size; a type-name string table (Default, String, Collision, Image, VertexObject, tfbActorInfo, PlacementReference, AbstractPlacement, igTransform, ...) sits between the table and the first section at 0x800 | (hexdump of tutorial level.bld.decoded 2026-09-12) sections sum to 30935720 bytes = file size; all fields big-endian<br>(ssa-archive scan strings) Type vocabulary shared by tutorial and challenge levels (10 851 common strings)<br>(bindiff/section-table comparison 2026-09-12) Challenge_Level_000 and _001 level.bld share the same 9-section layout (same alignments, last section ends at file size) while 20.9 MB of content differs: comparative diff across different levels is not informative, structure must be parsed per file | **LIKELY** | no | 2026-09-12 |
| igz.object-graph.accounting | Object enumeration by header pattern | level/*.bld:level.bld (decoded), object section (#1) @ section 1 (12) | {u32 type, u32 1, u32 0x01xxxxxx} headers, objects bounded by the next header | be | Header-pattern enumeration accounts for 100 percent of the object section on three levels: tutorial 21 680 objects (4 234 076 B) + 11 492 B lead-in; Challenge_000 11 908 objects + 5 604 B; Challenge_001 17 216 objects + 8 944 B. Object sizes are upper bounds (distance to the next header) until inline trailing data is understood. | (ssa-archive igz objects (src/igz/graph.mjs), tests/igz-fixtures.test.mjs) accounting objects+unparsed+padding == total on 3 levels; spawn record resolves as tfbPhysicsModel with its position field | **LIKELY** | no | 2026-09-13 |
| igz.object.id-remap | Object ids are relocated by a constant at load | level/*.bld:level.bld (decoded) @ object + 0x08 (and +0x08 of embedded records) (4) | u32 0x01xxxxxx per object header (+8) and per embedded sub-object | be | Superseded by igz.pointer.section-indexed: header word +8 is a section-indexed pointer (0x01 = section 2) to a string that many objects share; the "constant shift" 0x101C3408 is the MEM2 base of section 2 (0x911C3408) minus 0x80000000. Clones keep the source value. | (ssa-archive experiment ptr-scan (ptr-scan1.json)) ids of 5 objects: constant delta 0x101C3408<br>(ssa-archive experiment ptr-scan --dimensions (ptr-scan2.json, ptr-scan2-section1.bin) + igz fixups) 38 584 id words, one delta<br>[m3-level_027_tutorial-e3-1789254290299] CONTRADICTION: intermediate loader state of the spawn tfbPhysicsModel: word +8 0x0100ECC0 -> 0x911D20C8 = MEM2 address of section 2 (0x911C3408) + 0xECC0; the final 0x111D20C8 is that pointer with bit 31 cleared. 2 621 distinct values for 21 680 objects (4 300 tfbSpriteInfo share one): a shared string pointer, not an id | **UNKNOWN** | no | 2026-09-12 |
| igz.object.refcount | Header word +4 is the reference count: the number of pointer words that own the record | level/*.bld:level.bld (decoded) @ object + 0x04 (also embedded sub-objects) (4) | u32 header word +4 (1..7 in the tutorial); runtime 0x04080000 | count | be | For 21 213 of 21 680 detected records the word equals the number of confirmed pointer words (section 1 + section 5) targeting the record header; the 274 mismatches by -1 are the false headers the loader never touches, and the 98 table-listed records with one extra inbound word show that header-table entries are not counted (the table is not an owning reference). Shared embedded attributes match exactly (spawn owner +0x5C: 5 = 5 referrers, +0x78: 4 = 4, physics +0x70: 7 = 7). The spawn owner itself has refcount 1: its only owning reference is the back-pointer from its tfbPhysicsModel (+0x118); it is nevertheless constructed at load even when its table entry is redirected elsewhere, so construction does not follow ownership pointers from roots (see igz.loader.pointer-traversal for the revised model). A clone must carry consistent refcounts: 1 on a copied owner, and every shared target it points at should gain +1 (not done by igz clone-entity yet). | (scratch refcount.mjs over ptr-scan3-fixups.json) refcount == inbound confirmed pointers for 21 213/21 680 records; table-listed records: 2 701 equal, 98 off by one (table entry not counted)<br>(ssa-archive experiment ptr-scan (ptr-scan1.json)) runtime word +4 = 0x04080000\|count, incremented when the game takes references (tfbPhysicsModel +0x74 7 -> 10, RHSValueStack 1 -> 2) | **LIKELY** | no | 2026-09-13 |
| igz.placement.record-layout | Layout of script placement records (ScriptSet "set [set]||to" wrappers) and the header-table entry they embed | level/Level_027_Tutorial.bld:level.bld (decoded) @ 30 positioned instances in the tutorial file, e.g. 0x34959C/0x34AC18 (0x1C8), 0x34AA34 (0x1E4), 0x34D850/0x232258/0x2B7BF0 (0x198), 0x1E0490 (0x140); embedded type-104 entries at +0x48: 0x3495E4, 0x34AC60, 0x34AA7C, 0x34D898, 0x2322A0, 0x2B7C38 (456) | type-111 wrapper {+0x1C -> shared type-112 template class; +0x24/+0x3C/+0x44/+0x10C internal pointers; +0x48 embedded type-104 record whose offset is a header-table entry; +0x50 name; +0x6C f32 xyz; +0x7C f32 heading; +0x100 f32 scale; +0x174 f32 x3 bounds} with size-dependent tails: 0x1C8 (+0x1A0 model path, companions +0x128/+0x1AC/+0x1B0/+0x1B8), 0x1E4 (+0x124 -> type-64), 0x198 (none; push blocks +0xF0 -> type-92 script), 0x140 (+0xF0 script, +0x10C..+0x12C companions) | be | Every placement the header table lists is a type-104 record at +0x48 of a type-111 wrapper carrying the same name; the wrapper holds the transform and, depending on its size class, the model (inline path or type-64 reference), companions and an optional behaviour script. Same-size replacement therefore overwrites a table-listed record in place with another of the same type: the table, the count-bounded walks and the relocation stay consistent, which is why --replace-record works with zero shift. Pointer offsets come from the runtime fixup map (words rewritten in RAM), not from heuristics; the size classes were read on all 30 instances. CAUTION: the pointer layout is NOT fixed by the size class — PushBlock_Mabu (0x140) has pointers at +0x124/+0x128/+0x12C where Push_Block_Template(2) (0x140) has plain words; always read the pointer fields of both source and victim from the fixup map before a replacement. | (scratch layouts.mjs: runtime fixup map (ptr-scan3) pointer words per record, targets typed from their headers, header-table entries around each placement) identical pointer layout inside each size class; +0x48 is a header-table entry of type 104 for every placement checked; template-class record at +0x1C shared with refcount = number of placements of that class<br>(igz script / decodeScript on all 157 scripts (opcode text, section-2 strings, f32 triples)) 30 type-111 records carry a world-scale triple at +0x6C; names at +0x50 match the type-104 table-entry names; model path at +0x1A0 only in the 0x1C8 class (sunflower(1), weed(8)); the 0x1E4 class references its model through +0x124<br>[m3-level_027_tutorial-e3-1789304824550] moving +0x6C of the 0x1E4 instance (sunflower(2)) moved the visible plant; copying the 0x1C8 weed over the 0x1C8 sunflower slot showed a weed there — the layout reading holds on two size classes | **LIKELY** | no | 2026-09-13 |
| igz.placement.shared-model-record | Model records are shared between placements and live inside one placement's blob; a same-size replacement of that blob rewrites the model for every user | level/Level_027_Tutorial.bld:level.bld (decoded) @ 0x34ADB0 "C:/tfb/Content/Models/Objects/plants_weed2_whole.mdl" sits inside the weed_2_Template(8) wrapper blob (0x34AC18..0x34ADE0, i.e. victim+0x198) and is referenced by 25 placements outside that blob, e.g. weed_2_Template(10) 0x3D349C..(30) 0x3D5330; 0x3CFBE0 is the plant_flowerB_whole.mdl record; 0x349734 the plant_sunflower_whole.mdl record (248) | type-64 model record: +0x08 section-2 string = the .mdl path; addressed by the +0xDC field of every type-104 placement that uses that model; physically located inside ONE placement's blob (the first user), so it lies between two header-table entries that belong to another placement | be | A placement's blob is not private: records inside it can be shared by other placements. The CONFIRMED sunflower duplication (copying the sunflower wrapper over the weed_2_Template(8) wrapper) therefore also rewrote the shared weed model record at victim+0x198, renaming it from plants_weed2_whole.mdl to plant_sunflower_whole.mdl for the 25 other weeds of the level. The level played and the start island looked exactly as predicted because every affected weed is far from the spawn, but the side effect is real and an editor must report it. `planReplaceRecord` now lists `shared_records` (offset, number of external users, whether the copy changes its bytes, name before/after) and raises a validation warning for each one it rewrites. It also refuses a replacement when a header-table record inside the victim blob has no counterpart of the same type in the source. The t104-generic recipe (copying one standalone type-104 placement over another of the same span) touches no shared record at all, which makes it the cleaner duplication path when both placements exist without a type-111 wrapper. Generalised: in all five files tested, 100% of model records are physically inside some placement blob, so every replacement rewrites at least one model record; the safety rules therefore report the user count of each one and mark a rewrite of a shared one critical. Universal across the disc: every one of the 11 269 model records of all 76 levels lies inside some placement blob. | (fixup-map reverse index on the confirmed sunflower file: pointer words outside [0x34AC18, 0x34ADE0) whose target lands inside that range, and byte diff of the confirmed file) 25 external placements point at 0x34ADB0; the confirmed replacement changed 0x34ADB4 (refcount) and 0x34ADB8 (the .mdl path string id) of that record, plus the embedded type-104 header at victim+0x48 (2 external users: the Plants and Level_027_plants.lvl layers)<br>(igz placements / edit show on the 43 span-0x19C static placements) each weed placement's +0xDC points at the single shared record 0x34ADB0; the flowerB placements point at 0x3CFBE0; models are therefore per-template records, not per-placement copies<br>(igz models: shared-state census over five IGZ files) every model record of every file lies inside another placement’s blob: 210/210 in the tutorial, 38/38 in Challenge_000, 101/101 in Challenge_001, 134/134 in Mining, 11/11 in Gryphon. Shared by more than one placement: 61, 11, 23, 38 and 3 respectively; the most shared are Treasure_Coin_A.mdl with 42 users in the tutorial and barrel.mdl with 154 in Challenge_000. So a same-size blob replacement ALWAYS lands on a model record, and the only question is how many placements it is shared with.<br>(ssa-archive corpus over all 76 level archives) 11 269 model records across the disc, 2 691 of them shared by more than one placement, and 11 269 of 11 269 physically inside another placement blob. The caveat is not a tutorial quirk: on this disc it is universal. | **LIKELY** | no | 2026-09-13 |
| igz.section0.type-size-table | Type-name table in blocks with 12-byte block records, then a u32 table | level/*.bld:level.bld (decoded) @ 0x14E0 in the tutorial file (4-aligned end of the name list) (4) | u32[type count] | be | Section 0 holds the 224 type names then, at 0x14E0, a 176-word table of small values (0x0c, 0, 0, 0xA4, 0x2A8, 0x18, per-type instance sizes/vtable hints, high 90% agreement is not reached so sizes stay a hint, not authoritative) followed by ~75 000 words of 32-bit hashes (name/resource ids, not sizes). It is NOT a sequential record-size stream: cumulative sums from any start index land on almost no record headers, and its sum (12 146) is nothing like the object area (4 234 076). Section 0 is copied to MEM1 (0x814DA800) essentially unchanged at load (2 words of 76 384 differ): it is a lookup table read in place, not walked to build objects. Object sizes are therefore bounded only by the next header (igz.section1.object-list), which is why the loader must already know where records are before constructing them. | (research-probes/probe-igz-objects.mjs) 202 names counted between 0x834 and 0x14E0; a u32 array starts immediately after<br>(src/igz/types.mjs + igz types --histogram on the tutorial) Block record found at 0xA64; 21 687 objects enumerate with the NUL-split numbering and the M2-confirmed record is type 123 tfbPhysicsModel<br>(scratch sizestream.mjs / sec0.mjs over the tutorial + ptr-scan3 MEM1 dump) cumsum of the 176 small words lands on 0-3/170 headers for every start; section 0 runtime diff: 76 382/76 384 words identical | **LIKELY** | no | 2026-09-13 |
| igz.section0.type-table | IGZ v5 section 0 header and type-name table | level/*.bld:level.bld (decoded) @ 0x800 (52) | u32 words then NUL-terminated names | be | Section 0 (offset 0x800, the first descriptor) starts with a second "IGZ\x01" v5 header: words 5, 0x00020000, 0x1C, 0xE, 0x1C, 0, 1, 0, 0, 0x24, 0x248, 0x18, then the type names (metaobject, tfbLightInfo, tfbSpriteInfo, tfbParticleInfo, tfbSoundInfo, CameraInfo, ScriptSet, ActorWaypoint, ...). No TMET/TSTR/ROFS fixup magics exist anywhere in the section: the v5 layout predates the tagged fixups the community reader handles (its offset table for version 5 is empty). | (hexdump 0x800 of tutorial level.bld.decoded + magic search over section 0) Second header observed; 16 fixup magics searched, none found<br>(igArchiveExtractor IGZ_Structure.cs) locations[5] is an empty array | **LIKELY** | no | 2026-09-12 |
| igz.section1.object-list | Section 1 object pointer list | level/*.bld:level.bld (decoded) @ section #1 start (0x4B180 in the tutorial file) (16) | u32 count, flagged u32 pointer, u32[] absolute offsets | be | Words 0,0,0,0xB32 \| 0xB32, 0x80002CC8, 0x1C, 0x2CE4 then 79 consecutive absolute offsets 0xD8418 + 0x40*i pointing at 64-byte records of small integers and 24-bit values (attribute/enum records, not transforms); pointers with bit 31 set (0x8000xxxx) look like intra-section references. Interpretation UNKNOWN; 2866 objects listed | (research-probes/probe-igz-objects.mjs) Pointer arithmetic verified for 79 entries; records dumped | **UNKNOWN** | no | 2026-09-12 |
| igz.type92.script-record | Type-92 records are scripts, not entity wrappers; the spawn "owner" is the level-init script | level/*.bld:level.bld (decoded) @ all 157 header-table entries of type 92 in the tutorial (1) | type index 92 (named tfbProceduralUVAnimTrack in the section-0 type table): compiled script/behaviour record; header-table entry; blob = [entry, next entry) | be | Every one of the 157 type-92 table records references a compiled script (.ai path such as Activate_Jumpad.ai, Chompy_Sprite_, WaterVFX_Remover.ai, LootSystem_Init, InitializeLevel.ai) and script opcodes ("set value\|\|=", "check value", "create variable\|\|with\|members", "clone\|\|at\|facing\|\|cloned", PRESCRIPT/STARTUP/MACROS/INTERFACE sections). Their blob children (sprites, lists, placements, the spawn tfbPhysicsModel) are the script`s referenced resources and variables. 114/157 have more than five external pointers into their blob: scripts form a web, not self-contained entities. Consequences: (1) the M3 subject cloned so far — the type-92 "spawn owner" — is the level-initialisation script, effectively a singleton; duplicating it re-runs level init and freezes regardless of file layout, which explains why all four registration paths froze; the overwritten target 0x3EDEFC was the LootSystem_Init (STARTUP) script. (2) Entity duplication must target world entities reached through pointers, not table records. The section-0 type name for index 92 is therefore wrong or generic for this game. | (scratch generic-diag.mjs (header table + runtime pointer words + section-2 strings)) 157/157 type-92 records reference .ai script paths and opcodes; extIn>5 for 114<br>[m3-level_027_tutorial-e3-1789261575772] overwriting the LootSystem_Init script slot with a copy of the level-init script froze the load | **LIKELY** | no | 2026-09-13 |
| igz.types.per-file-indices | Type indices are per file and type names are not reliably aligned, so no cross-file tool may key on either | level/*.bld:level.bld (decoded), character/*.bld:level.bld (decoded) @ the placement class is index 104 in Level_027_Tutorial, 95 in Challenge_Level_000, 97 in Challenge_Level_001, 98 in Level_000_Mining and 57 in the 001_Gryphon character archive; its model records are 64 / 53 / 55 / 58 / 9 in the same order (4) | u32 type index in object header word 0, indexing that file's own section-0 name table; the same class has a different index in every file, and the parsed name/index alignment drifts | be | Two independent traps for any tool that reads more than one IGZ. First, the type INDEX is per file: hardcoding 104 for a placement reads igAlphaFunctionAttr or igGraphPathList in another level, which is why a first pass of the model resolver found 673 placements in the tutorial and zero everywhere else. Second, the type NAME is not a safe substitute: parseTypes splits section 0 on NUL and the name range is interrupted by 12-byte block records, so the index-to-name alignment drifts (1 358 of the tutorial's 2 858 header-table entries resolve to an empty name, and the placement class parses as igTransformSequence1_5 in the tutorial but as igAlphaFunctionAttr at the corresponding index elsewhere). The working rule is to DETECT a class structurally in each file, from the record layout, and to treat the parsed name as a label only. src/igz/model-resolve.mjs does this: it scores every class in the header table on a layout signature (world position at +0x24, heading at +0x34, scale 100 at +0xB8, a resolvable asset reference at +0xDC) and picks the dominant one, which is unambiguous in all five files tested (no runner-up scored above zero). Measured across the whole disc: 23 distinct indices hold placements depending on the file, which is why nothing may key on the number. | (scratch types.mjs / names.mjs / detect.mjs over five IGZ files of the sample disc) index 104 resolves to igTransformSequence1_5, SpriteInfoList, tfbSpriteInfo, SpriteInfoList and ContactInfo in the five files; looking the tutorial name up by name finds it at 104, 195, 197, 211 and absent, none of which match the structurally detected placement classes 104, 95, 97, 98, 57 — so the name table itself is misaligned<br>(igz models (structural detection) validated against the runtime fixup map of the tutorial) the structural detector picks type 104 in the tutorial and agrees with the runtime fixup map on 673/673 placements, then picks exactly one candidate class in each of the four other files with no runner-up<br>(ssa-archive corpus over all 76 level archives of the disc (docs/placement-corpus-all-levels.json)) the placement class is one of 23 different indices across the disc (76, 79, 86, 88, 91, 93, 94, 95, 96, 97, 98, 100, 101, 102, 103, 104, 105, 106, 108, 110, 111, 115 and 121), and the structural detector picks exactly one in 75 of the 76 files with NO second class scoring a single strict hit anywhere. The one file with no placement class is Title.bld, a menu scene with 173 header-table entries and no placed object at all. | **CONFIRMED** | no | 2026-09-13 |
| level.bld.nested-igz | Nested level.bld and language .pak entries | level/*.bld @ 0x0 (8) | IGZ file | be | Every decoded entry starts with 49 47 5A 01 00 00 00 05 ("IGZ\x01", 5); the nested level.bld (1.3 to 31 MB) is presumed to hold world data | (research-probes/probe-chunks.mjs) 7/7 entries of 3 archives start with IGZ magic | **LIKELY** | no | 2026-09-12 |
