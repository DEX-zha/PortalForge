# Findings — Visible geometry: meshes, vertices, materials, textures

Generated by `ssa-archive findings render` from `docs/findings/records/`. Do not edit by hand. Labels: CONFIRMED (experimentally demonstrated), LIKELY (concordant observations), UNKNOWN (hypothesis). Only CONFIRMED findings may be editable.

| Id | Structure | Location | Type | Endian | Meaning | Evidence | Confidence | Editable | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| igz.geometry.block-layout | Tutorial: 1586 interleaved blocks (world) and 678 separate-array blocks (props), 416 025 vertices, 526 834 triangles, 34 000+ strips; other primitives (0x90 list, 0xa0 fan, 0xa8/0xb0 lines) occur a handful of times. Mining: 925 blocks, 315 057 vertices. | level/*.bld:level.bld (decoded) @ tutorial section 6 at 0xf4f258; block 0 vertices at +0x24 (60 x 12 bytes), VAT header at +0x300 {6,14,0,0}, display list at +0x321 (9f 00 0b ...); block #1586 (a coin, 229 vertices) arrays at +0x5c0420 [10][4][4], VAT +0x5c1460 {10,6,14,14} (10506304) | section tag 51: after 32 zero bytes, one block per descriptor in descriptor order, every piece 32-aligned RELATIVE TO THE SECTION START (the section itself is not 32-aligned in the file). Interleaved kind (world chunks): {00 9F count(u16)} then count * stride bytes, position s16 x3 at +0. Separate kind (placed props): array 0 = position s16 x3 [+ normal s8 x3 + pad] of sizes[0] bytes per vertex, then one array per remaining size, no prefix. Then a 32-byte header of GX fraction bits, position first, one word per attribute (six words for a five-attribute mesh). Then 0x00 NOP and the display list: triangle strips {0x98|vat, count(u16), count * attrs * idxSize}, NOP separated, zero padded; idxSize 1 or 2 bytes, not a function of the vertex count (234 vertices with 16-bit indices seen); every attribute of a vertex uses the same index | be | The renderable geometry of a level, decodable without any name and without a runtime map. The walk is self-checking: a display list is accepted only if every index addresses a vertex of its own block and the list ends exactly where the next descriptor's block begins (its arrays laid from there end on a fraction header). That check is what disambiguates the index width, and what stops a next block whose raw bytes begin with an opcode-looking value (0xB2 was seen) from passing for one more strip. The first byte of a strip's 32-aligned start is always 0x00. | (scratch gxwalk3.mjs / gxmesh.mjs: sequential walk with both index widths tried and the next-block landing as the acceptance test) tutorial 2264/2264 blocks in sequence, section consumed to its end; Mining 925/925; disc 112/112 levels, 204 821/204 821 units after the opcode-range guard<br>(byte dumps at block boundaries: +0x2e0..+0x380 (block 0 end, header, first strips), +0x5d6ec0..+0x5d7300 (block #1595 arrays: 30 x 10, 30 x 4, 30 x 4, 30 x 4, then {10,6,14,0})) the 00 9F count prefix of interleaved blocks, the absence of any prefix on separate arrays, the 32-byte fraction header, and the NOP-separated strips are all visible in the raw bytes | **LIKELY** | no | 2026-09-14 |
| igz.geometry.descriptor-table | Tutorial: 2264 descriptors of 0x90..0x138 bytes (0x9c x1170, 0xb4 x477, 0xa8 x384 ...), strides 10..24; Mining: 925. The back-pointers land inside node records at fixed fields (+0xb8..+0xcc, +0x110 of the t8 nodes; +0x60/+0x74/+0xac of t12 nodes): 2264 on the tutorial, the words the loader rewrote in this section. | level/*.bld:level.bld (decoded) @ tutorial section 5 at 0xef281c; first descriptor at +0x04; sunflower head #2146, weed #2153 (379452) | section tag 44: one descriptor per draw unit, in file order; header {T, 1, count, 0x80000004, self-ptr, ...} where T is the per-file type index of the descriptor (0xf on the tutorial, read off the first entry at +0x04); +0x08 u32 vertex count; +0x34 u32 vertex stride; +0x44 u32 object-section offset of the owning node record (relocated by the game); +0x6c.. attribute list of triplets {tag<<24 | sub<<16, format, offset<<16} ended by 0x2c000000, tags 0x32 normal (s8 x3), 0x33 colour (2 bytes), 0x1d texcoord (s16 x2), 0x06 (4 bytes); after the LAST 0x2c000000 the record sizes of the separate arrays, e.g. {10,4,4}; an optional inner {T+1, 1, ...} block is a second material pass and owns no block | be | The table that tells the GPU what each mesh piece is made of, and the only link from a mesh to the object that owns it. The vertex count and the array sizes are what the block walk of section 6 needs; the back-pointer is what ties a piece to a placed model (igz.geometry.model-ownership). The attribute offsets are non-zero only for the interleaved (world) kind; the separate-array kind lists its arrays by size after the end marker. The position is implicit and always first. The descriptor type index is per file, like every type index (igz.types.per-file-indices). | (scratch field statistics over the 2264 tutorial descriptors (distinct values, monotonicity, alignment per word) and byte dumps of #0, #1, #5, #1586, #1590, #2146, #2153) +0x08 and +0x28 carry the same count; +0x34 equals the sum of the attribute sizes implied by the offsets (12 = 6 + 2 + 4, 19 = 6 + 3 + 2 + 4 + 4); the trailing size list after the last end marker sums to the stride on 2264/2264<br>(tools/ssa-archive/src/igz/gxmesh.mjs parseDescriptors + walkBlocks) using count and sizes from the descriptor, every block of section 6 is found where the previous one ends, on the tutorial (2264) and on Mining (925), and on all 112 levels of the disc (204 821 units) | **LIKELY** | no | 2026-09-14 |
| igz.geometry.editor-coverage | Tutorial: 2264 decoded units, 677 unique units assigned to placed models, 1587 omitted (1584 interleaved and 3 separate). Mining: 925 decoded, 279 assigned, 646 omitted (641 interleaved and 5 separate). Across 76 local *-all level workspaces: 126920 descriptors decoded, 94647 units omitted, including 94183 interleaved units. | level/*.bld:level.bld (decoded); tools/ssa-archive/src/editor/meshes.mjs @ assignUnits -> byModel -> modelMeshes -> meshesPayload -> scene.build (1) | decoded draw-unit coverage versus placed-model API coverage; read-only audit | be | The baseline editor omitted decoded scenery because meshesPayload serialized only placed-model geometry. The 2026-09-14 fix now serves unassigned interleaved units with fraction 6 as read-only world scenery: 1584 tutorial units and 641 Mining units, disjoint from placed-model units. Separate-array resources remain unresolved (3 tutorial, 5 Mining), including UI and ore-processing resources whose placement is not established. The browser draws scenery in batches at identity transform, outside placement picking and edits, under a dedicated Decor toggle. Oversized world units and placed models default to wireframe, with a reversible solid option, because their missing materials otherwise make backgrounds opaque and hide the editing area; this is a size-based preview treatment, not a semantic classification. complete=true still describes decoding only; world is the legacy unassigned count. assigned_unique + scenery + unresolved accounts for all decoded units without counting shared-model associations twice. Runtime visibility, collisions and geometry editing are not established by these offline and browser checks. | (tools/ssa-archive/research-probes/probe-mesh-coverage.mjs; .local/mesh-coverage-study/corpus-after.jsonl) After implementation, the same 76 archives decode 126920 descriptors without stopping. The API serves 92705 additional world-scenery candidate units and leaves 1942 unresolved, including 1478 interleaved units whose fraction is not 6. The conservative candidate rule deliberately leaves their world transform unclaimed. Unique model units plus scenery plus unresolved partition every file. Full regression suite: 219 tests pass, no skips.<br>(tools/ssa-archive/research-probes/probe-mesh-coverage.mjs; .local/mesh-coverage-study/coverage.jsonl and tutorial-runtime.jsonl) Tutorial and Mining decode their entire geometry sections. Tutorial produces identical assigned/omitted counts with structural references and the existing ptr-scan3 runtime fixup map. The actual mesh payload has only stats and models keys. Tutorial has 778 model-unit associations but only 677 distinct assigned units; association counts must not be used as coverage. Input hashes are recorded in the reports.<br>(tools/ssa-archive/research-probes/probe-mesh-coverage.mjs over 76 .local/workspaces/*-all/entries/3-level.bld.decoded files; .local/mesh-coverage-study/corpus.jsonl) 126920/126920 descriptors decoded without an early stop; 94647 decoded units absent from placed-model geometry, of which 94183 interleaved and 464 separate. This is the 76-file local sample, not a revalidation of the earlier session's 112-file census.<br>(tests/editor-scenery.test.mjs; .local/mesh-coverage-study/browser-check.mjs) Regression tests cover disjoint assignment, world coordinates, unresolved UI, index rebasing, disposal and placement-edit isolation. Actual WebGL checks on tutorial and Mining exercise scenery visibility, All/None, wireframe, selection of a real model and its movement without altering world geometry. Screenshots and result.json live in .local/mesh-coverage-study/browser-tutorial-bld/ and browser-mining-bld/. No new in-game run; finding remains LIKELY and not editable.<br>(.local/mesh-coverage-study/compare.mjs; placed-only.png and with-world-candidates.png) Same tutorial data and camera (eye 96,18,20; target 86,13,46): adding only unassigned interleaved units at identity transform restores island terrain and the windmill building in the diagnostic wireframe. This is an offline preview, not a new Dolphin run or an editor implementation. | **LIKELY** | no | 2026-09-14 |
| igz.geometry.position-quantisation | Tutorial: frac 6 x1589, 7 x19, 8 x16, 9 x9, 10 x631. Mining: 6 x641, 9 x2, 10 x282. | level/*.bld:level.bld (decoded) @ fraction header at the 32-aligned position after a block's arrays; tutorial block 0 {6,14,0,0}, block #1586 {10,6,14,14}, block #1859 {10,6,14,14,14,0,0,0} (32) | position = s16 * 2^-frac, frac being the first word of the block's 32-byte fraction header (the GX vertex attribute table shift); normals s8 with frac 6 (64 = 1.0, unit length verified), texture coordinates s16 with frac 14 (16384 = 1.0). World chunks use frac 6 in world space; placed props use 7..10 in the model's local space (a 1-unit coin: 10; a 6-unit sunflower: 10; a 10-unit windmill blade set: 10) | be | How the stored integers become coordinates. The check is against the owner's declared bounds ({4, 1, min xyz, max xyz} attribute of the node record): with the header's fraction, world chunk #2 dequantises to x -220.17..-218.64 against a declared box of -220.17..-218.64, a 0.01-unit error; per model, 209 of 209 tutorial models and 131 of 131 Mining models with declared bounds have every vertex inside them at 5%. A fixed fraction guessed from the extent would not do: the two kinds differ, and the props differ among themselves. | (scratch gxwalk3.mjs: best fixed fraction per block against the owner bounds, before the header was read) 2^-6 on 1548 world blocks with a median overshoot of 0.15% of the extent; the props then gave 2^-7..2^-10, and the header's first word matches the best fraction on every block checked<br>(tests/igz-gxmesh.test.mjs: 'every placed model of the tutorial owns a mesh that lies inside its declared bounds') passes on 210 models; the sunflower's 736 vertices lie within -1.52..2.25, -0.05..6.28, -1.57..2.40<br>(edit preview --meshes --eye 96,18,20 --target 86,13,46 against the Dolphin control screenshot m1-level_027_tutorial-reencode-1789234236226-control-46-tutorial-skylander.png) the decoded windmill blades, windmill, two sunflowers and floating island appear in the arrangement of the game's opening shot; a boot-judged prediction has not been run, which is what keeps this LIKELY | **LIKELY** | no | 2026-09-14 |
