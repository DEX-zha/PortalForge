{
  "id": "level.entity.duplicate-instantiated",
  "updated": "2026-09-13",
  "editable": false,
  "category": "world-entities",
  "confidence": "LIKELY",
  "endian": "be",
  "type": "a second, byte-identical instance of a PlacementReference chain node (0x70; texture reference \"globaltextures/checker.png\") constructed and post-load-processed by the engine",
  "structure": "First duplicated IGZ record instantiated as a live object (M3 mechanism)",
  "location": {
    "offset": "source P 0x123138 copied over its successor slot 0x1231A8 (victim: reference to globaltextures/format_tfl8/focus_mapper.png)",
    "file_pattern": "level/Level_027_Tutorial.bld:level.bld (decoded)",
    "length": 112
  },
  "meaning": "Run m3-…-1789301273731: replacing the 0x70 bytes of P`s successor with a copy of P (refcount 1, next = the victim`s old next) — six words changed in the whole file, no record moved, no table/count/size touched — produced a level that loads and plays (21 407 records constructed = healthy, tail roots constructed, 132 heap copies of the spawn position, Sonic Boom in the tutorial) with the copy constructed as a PlacementReference (class 0x8049C738 at the slot, reached through P.next) and processed by the post-load phase exactly like an original (+0x38 -> 0x04080002, +0x50/+0x54/+0x58 -> 1/0/0, the values a healthy P carries). Two live instances of the same record therefore coexist in the chain pred -> P -> copy -> 0x123218. This is the M3 mechanism: the engine instantiates a duplicated record when the copy occupies a slot that the count-bounded head walk already visits and is linked from a constructed predecessor. Limits: the subject is a texture reference (P = checker.png), so the duplicate has no visible effect and the victim (focus_mapper.png reference) is lost; a visible duplication needs a positioned entity and a free slot strategy (or the head-span count).",
  "evidence": [
    {
      "experiment_id": "m3-level_027_tutorial-e3-1789301273731",
      "summary": "slot 0x1231A8 live header 0x8049C738; P.next -> slot; slot.next -> 0x123218; 21 407 final records; tail roots 0x8048D788/0x80486F30; level playing (screenshot 46/52); slot post-load fields equal a healthy P"
    },
    {
      "probe": "static plan verification (scratch swapN.mjs / igz clone-entity --replace-node)",
      "summary": "6 words changed, all inside the victim block; file length unchanged; graph 21 680 objects before and after"
    }
  ]
}