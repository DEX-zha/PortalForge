# Findings — Spawns, triggers, doors, checkpoints, level exit

Generated by `ssa-archive findings render` from `docs/findings/records/`. Do not edit by hand. Labels: CONFIRMED (experimentally demonstrated), LIKELY (concordant observations), UNKNOWN (hypothesis). Only CONFIRMED findings may be editable.

| Id | Structure | Location | Type | Endian | Meaning | Evidence | Confidence | Editable | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| igz.script.clone-opcode | The engine`s own entity-spawn instruction, and what the M2-confirmed spawn record really is | level/*.bld:level.bld (decoded) @ 592 instances; 138 local to their script; extended form: InitializeLevel.ai instruction #2 at 0x1A76EC (+0x94 = (91.731, 10.31, 44.741)) (64) | opcode class type 123 (misnamed tfbPhysicsModel), text "clone||at|facing||cloned"; compact form 0x40 {7B, rc, name, 0,0,0, flags, 0, ->own+0x30 sub-object (type 0x7C), 0, ->B (type 114 record), ->C (type 102 record), 7C 1 1 1}; extended form 0x214 with an inline position at +0x94 | be | The CONFIRMED spawn record (level.physicsmodel.spawn-candidate) is instruction #2 of the level-init script InitializeLevel.ai: a "clone ... at ... facing" that clones the player character at the start position, which is why moving its +0x94 argument moved the Skylander (M2). The 137 compact clones (lanterns, bridges, chompy spawners, cannon balls, cracked-wall parts ...) carry no inline position: their asset/position/facing come from referenced type-114/102 expression records (shared, e.g. 0x188CBC / 0x187BA8, or local like 0x25D9C8 in Chompie_Spawner_Arena.ai). Consequences for a VISIBLE duplication: (a) duplicating a compact clone instruction spawns a second entity at the SAME computed position (overlapping); (b) a visible offset needs an instruction whose position is inline (the extended form) or a modified position expression; (c) any duplicate must be a same-size replacement of a sacrificable record inside the same script (the owner`s pointer array already addresses that slot), never an insertion. | (scratch clones.mjs / clonerefs.mjs over the script decoder) 138 local clone instructions, all type 123; compact refs -> own+0x30, type-114 and type-102 records; InitializeLevel #2 is the 0x214 form with the spawn triple inline<br>[m2-level_027_tutorial-e3-o1734528-1789248814839] moving +0x94 of that instruction displaced the Skylander spawn in 2/2 runs (M2 PASS) | **LIKELY** | no | 2026-09-13 |
| igz.script.record-model | How a script is stored: an instruction-pointer array in the owner, instruction records after it | level/*.bld:level.bld (decoded) @ all 157 type-92 header-table records of the tutorial (e.g. 027_HugoCS.ai at 0x2B633C: 204 pointers, 37 records, 0xE94 bytes) (4) | type-92 header-table record = one compiled script: owner header {+0x24 count, +0x28 capacity (== count), +0x2C 0x80000000|count*4, +0x30 -> own +0x34, +0x34.. count section-1 pointers}; the blob [entry, next entry) is tiled by the instruction records | be | Verified on 157/157 scripts (igz scripts): count == capacity, the size word is 0x80000000 \| count*4, the array starts at +0x34 and the blob is fully tiled by records (0 uncovered bytes). Each pointer designates either a local instruction record (in the blob) or a shared record/value elsewhere (variables, attributes, array elements — arguments by reference; refcount-N records the header heuristic misses). An instruction record`s word +0 is its opcode CLASS (type index) and word +8 its opcode TEXT in section 2; 287 distinct opcodes in the tutorial ("parameter" 8 310, "set value\|\|=" 7 140, "check value" 3 364, "set *reference\|\|to" 2 274, "use macro" 1 052, "displace\|\|\|LENGTH:\|HEADING:\|PITCH:" 644, "clone\|\|at\|facing\|\|cloned" 592, ...). The section-0 type names are therefore VM class names, not meanings: a "tfbSpriteInfo" (113) is a "set value" instruction, an "igIntKey" (100) a "parameter", a "ScriptSet" (111) a "set [set]", and "tfbPhysicsModel" (123) is the CLONE opcode class. Tool: igz script <decoded> <record> --fixups lists a script; igz scripts audits the model. | (igz scripts (tutorial level.bld, ptr-scan3 fixup map)) 157/157 scripts: issues 0, uncovered 0<br>(igz script 0x2B633C (027_HugoCS.ai)) 204 pointers: PRESCRIPT, set value "HitState", set [set] "LocalOuchSounds", parameters shutdown/destructible/hp/unkillable, check/play/set *reference ..., last instruction OpUseMacro "move to" with 4 waypoints | **LIKELY** | no | 2026-09-13 |
| level.entities.script-spawned | Visible dynamic entities are spawned by script bytecode, not placed as records | level/*.bld:level.bld (decoded) @ type-92 script records (157) and section 5 (second object section, 4 584 class pointers); OpCodeList x45 are small macro opcode lists (0x3C–0x18C) (1) | where visible dynamic entities come from in level.bld | be | Section 1 holds no per-prop transform table (matrix search: 913 false positives owned by igVertexFormat/ScriptSetReference/igIntKey, none near the spawn except two round-valued igIntKey script values), the type-42 object at 0x115B50 (count 0x5DA) is a bounding-volume tree (its first pair is the whole-level AABB; its -259.26 was the first, effect-less M2 pipeline test), and the type-52 nodes name assets (.lvl) rather than instances. Static props are therefore baked into the level mesh, and dynamic actors (chompies, barrels, Hugo) are created by scripts: the engine exposes a "clone\|\|at\|facing\|\|cloned" opcode, and script arguments carry world positions (Hugo waypoints). Consequence for a VISIBLE duplication: duplicate a spawn instruction (or move its position argument first, M2-style), which requires decoding the script bytecode format (type-92 blob bodies / section 5) — a static chantier before any boot. | (scratch visible.mjs / opmacro.mjs / ops.mjs / opcodelist.mjs) no transform table in section 1; type-42 0x115B50 = AABB tree; type-52 = 176 asset names x 11; Op* headers: OpUseMacro 50, OpStartUp 50, OpCodeList 45 (small), OpSpawn only as a false header<br>(igz near 91 10 44 --tol 30) triples near the spawn are trigger boxes (ScriptSetReference), ScriptSet positions, script arguments (OpUseMacro) and the tfbPhysicsModel spawn | **LIKELY** | no | 2026-09-13 |
