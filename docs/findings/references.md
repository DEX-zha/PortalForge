# Findings — Inter-resource references, fixups, tables and counts

Generated by `ssa-archive findings render` from `docs/findings/records/`. Do not edit by hand. Labels: CONFIRMED (experimentally demonstrated), LIKELY (concordant observations), UNKNOWN (hypothesis). Only CONFIRMED findings may be editable.

| Id | Structure | Location | Type | Endian | Meaning | Evidence | Confidence | Editable | Updated |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| igz.clone.in-place-registration | Registering a clone without growing the header table or shifting the file | level/*.bld:level.bld (decoded) @ a chosen type-92 header-table entry whose blob >= the clone block (1) | method: overwrite an existing same-type header-table record with the clone block | be | Because relocation is reflection-driven (igz.loader.relocation-table), a same-type record in an already-registered slot needs no new relocation data. Growing the header table to register a clone shifts every object 32 bytes and desynchronises the load (froze at 1 036/21 680 records), so registration must not move anything. `igz clone-entity --overwrite <T>` writes the clone block over an existing type-92 table record T (rebasing block-internal pointers to T, zeroing the blob leftover, incrementing the refcounts of the shared objects the clone points at), leaving T`s table entry and every other byte untouched. On the tutorial, target 0x3EDEFC (chosen with igz pick-overwrite-target: blob 0x500, 0 external references into its interior) yields a VALID plan whose diff against the original is exactly the 0x2A8 clone block plus five 4-byte refcount words at the shared externals (0x187BF0, 0x187E74, 0x188CC0, 0x188CDC, 0x1893EC); the file length is identical. This is the registration path to test next; it sacrifices one benign type-92 record to host a duplicate of the spawn entity. | (ssa-archive igz clone-entity --overwrite (static, entity-overwrite-plan.json) + byte diff) VALID plan; only [0x3EDEFC,0x3EE3FC) and 5 refcount words differ; file length unchanged; clone owner and physics recognised at the target<br>(ssa-archive igz pick-overwrite-target) 129 type-92 table entries with blob >= 0x2A8; 0x3EDEFC has 0 external references into its blob<br>[m3-level_027_tutorial-e3-1789261575772] CONTRADICTION of the "registration is enough" hypothesis: the in-place overwrite onto 0x3EDEFC loaded with the file length unchanged and nothing shifted, and the ORIGINAL spawn constructed normally (class pointer 0x80486514), but the load still froze on "Loading" and the clone`s position (99.73) was absent from MEM1 entirely — construction hangs before reaching/finishing the duplicated record. Registering a same-type record in a valid slot is necessary but NOT sufficient; duplicating a type-92 entity is blocked by a deeper construction constraint (candidate: the type-92 walk iterates children by the blob/size or a count and mis-parses the zeroed leftover, or a duplicated record conflicts on an identity/handle/dependency that in-place M2 mutation never triggers). | **LIKELY** | no | 2026-09-13 |
| igz.flagged-word.global-handle | Flagged words are indexes into a global handle table, not section pointers | level/*.bld:level.bld (decoded) @ object fields (e.g. PlacementReference +0x18, tfbPhysicsModel +0x48, type-52 node +0x34) (4) | u32 with bit 31 set, low bits small (0x80000000, 0x80000004, 0x8000000A, 0x80000028, 0x800000CF, 0x800001A7) | be | At load the flagged words become pointers to MEM1 addresses outside the resident section (0x80000000 -> 0x807AF3C0, 0x8000000A -> 0x806DFAF8, 0x800000CF -> 0x80990F44): the low bits index a global/shared resource table (materials, handles from other archives), so they must not be rebased when section 1 is relocated. Their exact table is UNKNOWN. | (ssa-archive experiment ptr-scan (ptr-scan1.json)) tfbPhysicsModel +0x48 0x800000CF -> 0x80990F44; type-52 +0x34 0x80000000 -> 0x806F9F94; PlacementReference +0x18 0x80000000 -> 0x807AF3C0<br>(ssa-archive experiment ptr-scan --dimensions (ptr-scan2.json, ptr-scan2-section1.bin) + igz fixups) type-52 root 0x457588 +0x34 0x8000000A -> 0x806DFAF8; PlacementReference 0x1230C8 +0x18 -> 0x807AF3C0 (same global) | **LIKELY** | no | 2026-09-13 |
| igz.loader.blob-walk-stomps-insertions | Why inserting bytes anywhere inside the object span breaks the load: the enclosing table record grows and its constructor walks the inserted bytes | level/*.bld:level.bld (decoded) @ any table-record blob in section 1 (1) | behaviour of table-record constructors (type 52 memory nodes at 0x40 stride with an embedded sub-object at +0x1C; type 92 scripts): they walk their whole blob = [entry, next table entry) | be | Run m3-…-1789299359139 (PlacementReference clone inserted at 0x457588, right after the type-52 table record 0x457548): the loader finished the level (21 402 records constructed, same as a healthy load) and the clone WAS constructed with the PlacementReference class pointer through P.next — the first clone ever instantiated — yet the load hung and the six tail root records (2 type-52 + 4 RHSValueStack) stayed raw. The clone body carries the exact signature of the type-52 constructor: class pointers written at clone+0x1C, +0x40 and +0x5C, and the texture tag "TFL8" at +0x44 rewritten as a refcount (0x54464C38 -> 0x040E4C38) — i.e. the type-52 record at 0x457548 saw its blob grow from 0x40 to 0xB0 and walked the clone as two of its own 0x40 nodes (node at clone+0x00 with embedded +0x1C, node at clone+0x40 with embedded +0x5C). Constructing the clone twice (reflection via P.next, then the stride walk) derailed the tail walk, the root records never constructed and the post-load init hung. General rule: every byte of the span belongs to some table record`s blob, and at least types 52 and 92 walk their blob positionally, so ANY insertion inside the span corrupts the enclosing record. Safe placements are therefore outside every walked blob: the section end (the last blob tolerated appended bytes intact in run m3-…-1789256664691) with reachability provided by a pointer from a constructed record. | [m3-level_027_tutorial-e3-1789299359139] clone at 0x457588 constructed (class 0x8049C738 via P.next), 6 tail roots raw, clone body stomped with type-52 node signature at +0x1C/+0x40/+0x44/+0x5C; hung on Loading<br>[m3-level_027_tutorial-e3-1789257261106] earlier: script clone at the same insert point was sized 0x2A8 = merged with the following blob and the tail partially constructed — same mechanism<br>[m3-level_027_tutorial-e3-1789300434156] same stomp signature reproduced from the OLD table start after a head-span insertion: the type-52 walker start is derived from a record count, not from the (rebased) table entry | **LIKELY** | no | 2026-09-13 |
| igz.loader.enumerated-span | Objects live only inside the enumerated span; embedded children are built by their owner | level/*.bld:level.bld (decoded) @ section 1, first object (block end +0x1C) to the last record before the header table area (1) | the contiguous object span [first object, last constructed object] that the loader instantiates | be | Three boots pin the enumeration: (1) a clone appended AFTER the last object stays raw and the level is fine; (2) a clone inserted BEFORE the tail objects is constructed; (3) redirecting a table entry to an unconstructed record hangs the post-construction table pass. So construction walks a contiguous span of records (not the header table, not a size stream, not ownership pointers from roots), each top-level record building the children embedded after it. To duplicate an entity the copy must land inside the span (igz clone-entity --insert-before) AND its embedded child must construct without error; the current clone hangs while building the child, which is the remaining M3 blocking unknown (candidate causes: a resource handle/flagged 0x8xxxxxxx field the child constructor dereferences, or a count/pointer inside the physics record that must be re-pointed, not just rebased). | [m3-level_027_tutorial-e3-1789256664691] append-after-last: clone raw, level alive (132 heap copies of the spawn position)<br>[m3-level_027_tutorial-e3-1789257261106] insert-before-tail: clone owner constructed, embedded physics raw, load hung<br>[m3-level_027_tutorial-e3-1789257261106] confirmed: clone owner constructed as a 0x2A8 blob (= owner 0x94 + physics 0x214), the loader walked to the next table-listed record; the type-92 record owns a contiguous blob including its embedded tfbPhysicsModel<br>[m3-level_027_tutorial-e3-1789299899074] append-after-last WITH a rebased pointer to the copy: still raw. The span is defined by the table-record blobs, not by reachability; bytes past the last blob are never constructed even when pointed at. | **LIKELY** | no | 2026-09-13 |
| igz.loader.fixup-map | Load-time fixup of every visited IGZ object (section 1 resident at 0x80DBC020 + file offset) | level/*.bld:level.bld (decoded) vs MEM1 while the level is loaded @ every object header and pointer field of section 1 (4) | per-word rewrite rules observed between file and resident section 1 | be | For each object the loader reaches: word +0 (type index) becomes a class pointer 0x8048xxxx-0x804Axxxx (66 classes seen), word +4 (refcount 1..7) becomes 0x04080000\|refcount (incremented when the game takes references), word +8 (0x01xxxxxx id) becomes id + 0x101C3408, every section-relative offset (157 690 words) becomes 0x80DBC020 + 0x4B180 + value, string offsets (15 281 words) become pointers to interned strings outside the section, and class constructors fill runtime fields (45 206 function pointers, 33 623 filled words such as the spawn position copied from +0x94 to +0xAC of the tfbPhysicsModel). Words that are none of these stay byte-identical. The map is exact for the tutorial level and reproducible offline with `igz fixups <decoded> <dump>`. | (ssa-archive experiment ptr-scan (ptr-scan1.json)) 5 targets read back after state slot 6: type words -> 0x80485f48/0x80486514/0x80486f30/0x8049c738, ids +0x101C3408, 30 section-relative words rebased to base+offset, section-2 strings interned in MEM1<br>(ssa-archive experiment ptr-scan --dimensions (ptr-scan2.json, ptr-scan2-section1.bin) + igz fixups) Full 4 253 760-byte resident dump: 21 408/21 680 objects rewritten, 157 690 pointer words, 38 584 id words, 15 281 string words, 69 751 class words classified; 583 569 words unchanged | **LIKELY** | no | 2026-09-13 |
| igz.loader.head-span-count-walk | Head-span construction is count-bounded: inserting a record there shifts every later walker start by one record | level/*.bld:level.bld (decoded) @ section 1, [0x4DE64, 0x123598) in the tutorial (1 680 refcount-1 records, 1 727 headers of any refcount) (1) | the region [first object, first header-table record) is constructed by a positional walk that ends after a FIXED number of records, then the type-52 array walk (0x40 stride, embedded sub-object at +0x1C) starts where that walk ends | be | Run m3-…-1789300434156 inserted a 0x70 PlacementReference copy inside the head span (at N, shifting N and all 20 007 later records by 0x70 with every pointer word rebased, table entries included). Result: the whole head span constructed (P, copy, N, all placements up to 0x123598) and the copy itself was constructed (class 0x8049C738), but the type-52 array walker started at 0x123598 — the OLD address of the first table record — although table entry #0 correctly said 0x123608: it walked the last head placement and the shifted type-52 array with a 0x10 misalignment (class writes at 0x1235B4, 0x1235D8, 0x1235F4, 0x123618, 0x123634 …, i.e. nodes every 0x40 from 0x123598 with embedded sub-objects at +0x1C), 10 397 table records stayed raw, the tail roots never constructed and the load hung. No stored offset or length reproduces 0x123598 (searched every section, the file header and non-rebased object words), so the head walk is bounded by a record COUNT the file does not expose as a plain word (candidates 1 680 / 1 727 were not found as meaningful fields), or by a count derived at runtime. Consequence: records cannot be INSERTED into the head span without also growing that count; a same-size REPLACEMENT of an existing head record keeps every count, size and table intact. | [m3-level_027_tutorial-e3-1789300434156] copy constructed, head span constructed, type-52 walker started at old address 0x123598 (0x70 short), 10 397 table records raw, tail roots raw, hang<br>(scratch findstart.mjs) no u32/u16 in the header, sections 0/2-8 or non-rebased section-1 words equals the first-table-record offset (abs/rel), the head-span length, or a record count in a list-header position | **LIKELY** | no | 2026-09-13 |
| igz.loader.pointer-traversal | How the engine finds the objects it constructs: pointer traversal from the section-1 header roots | level/*.bld:level.bld (decoded) @ section 1 + 0x1C (first object), + 0x20.. (table), block before the first object (4) | traversal order of the loader (graph reachability, not sequential) | be | The loader does not walk section 1 sequentially: 272 objects scattered between 0x186888 and 0x450C18 (CameraInfo 15, tfbActorInfo 15, ScriptSet 9, ValueInfo 11, Op*/Measurement/Slider script types ...) keep their raw file bytes in RAM while their neighbours are rewritten; 179 of them have no incoming pointer word at all. Every object with an incoming pointer from a rewritten object is itself rewritten. An object appended to section 1 without an incoming pointer is therefore never fixed up, never constructed and never becomes a gameplay entity: this is the structural cause of the first M3 failure (clone appended and written into a word inside an object, not into any list). Making a clone alive requires a pointer from a visited structure: for the spawn owner that structure is the section-1 header table (entry #1074), so the reachable clone adds a table entry and rebases all 160 575 shifted pointer words. | (ssa-archive experiment ptr-scan --dimensions (ptr-scan2.json, ptr-scan2-section1.bin) + igz fixups) 272 unvisited objects scattered among 21 408 visited ones (18 373 visited objects lie inside the unvisited range); 179/272 have no confirmed pointer to them<br>(static reachability over the confirmed pointer words (scratch reach-vs-live.mjs)) traversal from table+block roots over the 157 690 confirmed pointer words reaches 19 848 objects; 191 objects are both unreachable and unvisited; remaining differences come from the first-object root and inline lists<br>[m3-level_027_tutorial-e3-1789256232052] CONTRADICTION of the root hypothesis: with table entry #1074 redirected from the spawn owner to an appended clone (no relocation), the original owner and its physics model were fully constructed (class pointers, position copied) while the clone stayed raw and the load froze. Table entries do not drive construction; whatever enumerates objects reached the owner without any file pointer to its header other than its child back-pointer.<br>[m3-level_027_tutorial-e3-1789257261106] Clone block inserted INSIDE the enumerated range (before the tail objects), no table entry, refcounts bumped: the clone OWNER was fully constructed in RAM (class pointer 0x80485F48, id remapped, +0x18 rebased) — so being inside the range is what makes a record enumerated — but its embedded tfbPhysicsModel child (owner+0x94) stayed RAW and the load hung. The loader reaches records by position in the enumerated span, and an owner constructs its embedded children itself; the hang is inside constructing the cloned child. | **LIKELY** | no | 2026-09-13 |
| igz.loader.reflection-constructs-pointees | Pointer fields are rebased by reflection, but a pointee is constructed only by the positional walk of the table-record blob that contains it | level/*.bld:level.bld (decoded) @ PlacementReference +0x64 (next link) as the test case (4) | a record pointed at by a constructed record`s pointer field is constructed (class pointer assigned, fields rebased) even when it is not a header-table entry | be | Two boots with the same PlacementReference copy linked through P.next: inside a type-52 blob it was constructed (by that blob`s stride walk reading the copy`s type word) and stomped; appended outside every blob it stayed raw while P.next pointed at it and the level played normally. Therefore: (1) construction = each header-table record walks its blob [entry, next entry) and constructs every sub-record it lands on by its type word; (2) reflection only rebases pointer fields; pointing at a record never constructs it; (3) a raw pointee does not crash the game (inert). To duplicate a placement, the copy must sit INSIDE the blob of the table record that already owns P and N, at a position that blob`s walker reaches with the correct stride — i.e. inserted at N`s position, shifting N and the rest of the section by the record size, with every pointer word rebased from the fixup map. | [m3-level_027_tutorial-e3-1789299359139] P.next -> 0x812135A8 (rebased), deref -> 0x8049C738 (PlacementReference class): clone constructed; chain pred -> P -> clone -> N all constructed<br>(ptr-scan1/2/3 fixup maps) PlacementReference class pointer is 0x8049C738 for every constructed instance; +0x64 words are runtime-confirmed pointers<br>[m3-level_027_tutorial-e3-1789299899074] CONTRADICTION: CONTRADICTION: the same PlacementReference copy appended at the section end (outside every table-record blob) and linked through P.next was NOT constructed: P.next was rebased to the copy (0x812139E0 = base + 0x4579C0) but the copy header stayed raw (0x0000000C). The level loaded and played normally (no freeze, tail roots constructed), so a raw pointee is tolerated but inert. Reflection rebases pointer fields; it does not construct what they point at. In run 1789299359139 the copy was constructed by the type-52 blob walk that read its type word, not by reflection. | **LIKELY** | no | 2026-09-13 |
| igz.loader.relocation-table | The pointer relocation is computed at load from per-type field metadata, not read from a flat table | level/*.bld:level.bld (decoded) @ unknown; not stored as raw offsets, cumulative sums, a bitmap, or a byte/u16 delta stream in sections 0,2-8 (1) | reflection-driven: per-type field descriptors (igMetaField-style: field offset + is-pointer + array-count) walked per object, NOT a flat stored offset list | be | Static probe (igz relocation-probe) result: the 162 110 relocated section-1 words are reproduced by NO flat encoding — not raw offsets, cumulative deltas, LEB128, byte/u16 delta streams, nor a bitmap, in any of sections 0,2-8 (best recall < 2%); there is no named fixup section (no ROFS/RVTB/etc. magic; sections end exactly at EOF). Instead a structural model explains 76.2% directly (29.2% at type-consistent fixed field offsets, 47.1% inside contiguous pointer arrays whose length varies per instance) and the remainder are per-instance pointers in variable-layout records. This is the signature of REFLECTION-DRIVEN relocation: each type carries field descriptors (offset, is-pointer, and for list fields a count to follow), and the loader walks every processed object`s fields by its type to rebase them (Alchemy igMetaObject/igMetaField). Consequences for M3, reframed: there is NO flat relocation list to decode and extend; a clone of an EXISTING type needs no new relocation data — it only needs to be PROCESSED by the same per-type field walk as the originals, which is gated on being a first-class record (a header-table entry). The remaining task is therefore (a) locate the per-type field descriptors in the section-0 metadata to model relocation to 100% and, (b) register the clone without the global-shift desync that broke the header-table growth attempt (m3-…-1789258792820). Tooling: `igz relocation-probe <decoded> --fixups <map>` scores encoding hypotheses and the structural model on every section. UPDATE 2026-09-13: after decoding relocation as reflection-driven, four registration paths were tried on a real clone — grow the header table (global 32-byte shift -> desync, froze at 1 036/21 680), redirect an existing entry to an appended clone (froze), insert inside the span without a table entry (owner constructed, fields not rebased, froze), and in-place overwrite of a same-type record with no shift and file length unchanged (original spawn constructed, but clone absent from RAM, froze). All freeze on Loading. The blocker for M3 is therefore NOT purely the relocation/registration of the clone`s bytes; it is a deeper constraint in constructing a DUPLICATED type-92 spawn-owner entity, which in-place single-field mutation (M2, PASS) never triggers. Duplicating this entity is not achievable with byte-level file editing at the current level of understanding. | [m3-level_027_tutorial-e3-1789257261106] clone inside span, no table entry: clone owner constructed (vtable) but its field pointers NOT rebased (owner+0x3c stays 0x40c49c), physics child never built -> hang<br>[m3-level_027_tutorial-e3-1789258792820] clone inside span WITH a table entry (32-byte global shift): relocation desynced globally, only 1 036 records constructed, header type words wrongly rebased (physics +0 0x7b -> base+0x7b), early hang<br>(ssa-archive igz relocation-probe (reloc-probe.json)) no flat/bitmap decoder exceeds 2% recall on any section; structural model (type fixed fields + array runs) explains 76.2%; no fixup-section magic; file ends at the last section | **LIKELY** | no | 2026-09-13 |
| igz.pointer.section-indexed | Section-indexed pointer encoding shared by all IGZ v5 words that become pointers at load | level/*.bld:level.bld (decoded) @ any pointer field; object header word +8 (0x01xxxxxx -> section 2 string) (4) | u32: high byte = target section index among sections 1..8 (0 = section 1, 1 = section 2 ...), low 24 bits = offset in that section; 0x80xxxxxx = global handle | be | The loader resolves a word by taking sections[high byte + 1].resident_address + low 24 bits: 0x00xxxxxx are section-1 offsets (the 157 690 plain pointers), 0x01xxxxxx point into section 2 (strings; header word +8 is such a pointer, e.g. "Levels/Level_027/027_ART_MASTER.ma" for tfbLightInfo, "_worldLight1" for the RHSValueStack root, "balloonist.png_ALPHACHANNEL" for a PlacementReference), 0x02000010 in tfbLightInfo +0x18 became section 3 + 0x10, and words with high bytes 3..7 (4 598 into section 5, 902 into section 7 ...) target the other MEM2 sections. A first pass rebases every such word (an intermediate state 80E0721B 00000001 911D20C8 80E071A0 was caught on the spawn record when a relocated level froze), later passes turn word +0 into the class pointer, word +4 into flags and clear bit 31 of the section-2 pointers. Consequence for relocation: inserting bytes in section 1 must rebase only words whose high byte is 0 (in every section), and words pointing into section 1 from sections 2..8 are candidates that the first relocation attempts did not rebase. | [m3-level_027_tutorial-e3-1789254290299] intermediate loader state caught in RAM: word +8 -> section-2 MEM2 address + offset; word +0 (type 0x7B) -> section-1 address + 0x7B; word +0xC 0 -> section-1 address<br>(ssa-archive experiment ptr-scan (ptr-scan2.json) + scratch survey) tfbLightInfo +0x18 0x02000010 -> 0x911F6C20 (section 3 resident address + 0x10); high-byte census of section-1 words: 18 776 -> section 2, 24 -> 3, 493 -> 4, 4 598 -> 5, 158 -> 6, 902 -> 7, 268 -> 8; 271 header words +8 beyond section 2 belong to the 272 objects the loader never touches (false headers) | **LIKELY** | no | 2026-09-13 |
| igz.section1.header-list | Section-1 header list (count 0xB32) and its null slots | level/*.bld:level.bld (decoded) @ section 1 + 0x0C/0x10 (count), + (word 0x14 & 0x7FFFFFFF) offsets array (4) | u32 count, flagged u32 pointer, u32[count] section-relative offsets (0xFFFFFFFF = null, 35 in the tutorial) | be | Superseded by igz.section1.header-table: the array of section-relative offsets lies between the 8 header words and the flagged end word; there are no null slots. The first M3 clone was therefore never listed anywhere, which, with igz.loader.pointer-traversal, explains why it stayed inert. | [m3-level_027_tutorial-e3-1789250807367] Clone + null-slot list entry: level loads (15 341 kB served), scene identical to control in 2/2 runs<br>(ssa-archive scratch table-census.mjs (2026-09-13)) CONTRADICTION: the offsets array is BEFORE the flagged word (which is its end), so the 35 "null slots" were 0xFFFFFFFF words inside objects and the M3 list write landed inside an object; superseded by igz.section1.header-table | **UNKNOWN** | no | 2026-09-12 |
| igz.section1.header-table | Section-1 header table: registration list of the refcounted record family, root of the loader traversal | level/*.bld:level.bld (decoded); tutorial: 0x4B180..0x4DE64 @ section 1 + 0x00 .. + (count*4) + 0x1C (4) | u32[8] header + u32[count-8] section-relative offsets + 0x1C-byte block | be | Header words: {0, 0, 0, count, count, 0x80000000\|count*4, 0x1C, count*4+0x1C}; count (0xB32 = 2 866 in the tutorial) counts u32 words from the section start including the 8 header words, so the offsets array holds count-8 entries (2 858) and the flagged word is the table end; the block of 7 words that follows holds 6 more section-relative roots (two 0x40-byte type-52 nodes and four RHSValueStack objects at the end of the section) plus 0x800001A7. Every entry is a section-relative offset of a record whose header is {type, refcount 1..6, 0x01xxxxxx}; the family is types 52, 54, 58, 83, 90, 92, 104, 151, 160 (all instances of 52 and 92, e.g. the spawn owner at entry #1074), 68, 88, 151. There are no null (0xFFFFFFFF) slots: the 35 nulls reported earlier were words inside objects, because the array had been placed after its end pointer. At runtime the header becomes an object (class 0x80493758 at +4, count words zeroed, +0x1C -> first object) and every entry becomes a pointer; a heap array copies {first object, entries...}. Growing the table by one entry is possible with the fixup map (igz clone-entity). | (ssa-archive scratch census (table-census/table-words/table-targets.mjs)) 2 858 entries: 1 123 land on {type,1,id} headers, 1 735 on {type,2..6,id} records inside detected objects; target word0 in {52,54,58,83,90,92,104,151,160}; ascending in stride groups; 0 nulls<br>(ssa-archive experiment ptr-scan --dimensions (ptr-scan2.json, ptr-scan2-section1.bin) + igz fixups) 2 865 head words rebased to MEM1 pointers (2 858 entries + 6 block roots + 0x1C), count words zeroed, +0x04 -> class 0x80493758<br>[m3-level_027_tutorial-e3-1789256232052] Redirecting entry #1074 to an unconstructed appended object froze the load after the original objects were constructed: the table is post-processed after construction and expects constructed records (memory blocks with refcounts); it is not the construction root list | **LIKELY** | no | 2026-09-12 |
| igz.section5.object-section | Section 5 is a second object section whose objects point into section 1 | level/*.bld:level.bld (decoded) @ section 5, whole section (4) | section 5 (tag 44, align 4, 0x5CA3C bytes in the tutorial): objects {type, refcount, ...} from +0x04 with section-indexed pointers | be | At load, section 5 (resident right after section 4 in MEM2, 0x91C5E280 in the tutorial state) is rewritten like section 1: 4 584 type words become class pointers (0x8049F228 ...), refcounts become 0x0488000N, 7 582 words 0x04xxxxxx become pointers into section 5 itself and 2 320 words 0x00xxxxxx become pointers into section 1 (e.g. +0x48 0x47F8 -> 0x80E0B998). Section 7 (tag 65) also holds objects (473 classes, 72 self pointers) but none into section 1; sections 2, 4, 6, 8 are never rewritten (strings, geometry, textures). Any relocation of section 1 must therefore rebase these 2 320 section-5 words too (igz fixups --regions, cross_pointer_words), which the first two reachable-clone attempts missed. | (ssa-archive experiment ptr-scan --dimensions (ptr-scan3, MEM1+MEM2 dumps) + igz fixups --regions) section 5: 28 924/94 863 words changed; by target: 2 320 -> section 1 (plain, delta 0), 7 582 -> section 5; 4 584 class pointers; 1 786 filled MEM2 pointers<br>(static census of section 5 words) header words {0, 0xF, 1, 0x3C, 0x80000004, 0x0400002C, ...}: high byte 4 = section 5 self references consistent with the section-indexed encoding | **LIKELY** | no | 2026-09-13 |
| level.bld.resident-in-mem1 | Decoded level.bld loaded verbatim into MEM1 | RAM while level/Level_027_Tutorial is loaded @ 0x80DBC020 + file offset (1) | byte image | be | Section-1 bytes of the decoded nested level.bld are found in MEM1 at file_offset + 0x80DBC020 (three independent 32-byte patterns, one match each). Live memory edits through dolphin_write_float therefore target the same bytes an archive mutation would change; a native state saved at the tutorial start (slot 6) allows fast repeated probes | (ssa-archive experiment live-probe (live-probe1.json)) 3/3 patterns matched once with consistent deltas; poke verified by read-back<br>(ssa-archive experiment ptr-scan (ptr-scan1.json, ptr-scan2.json)) two further boots from state slot 6: every rebased pointer equals 0x80DBC020 + file offset; heap descriptor at 0x81220634 lists section 1 at 0x80E071A0 (MEM1) and sections 2..8 in MEM2 (0x911C3408 ...) | **LIKELY** | no | 2026-09-12 |
